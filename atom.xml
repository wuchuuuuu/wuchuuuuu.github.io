<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>DBA有话说</title>
  
  
  <link href="http://wuchuuuuu.github.io/atom.xml" rel="self"/>
  
  <link href="http://wuchuuuuu.github.io/"/>
  <updated>2025-08-21T12:30:50.403Z</updated>
  <id>http://wuchuuuuu.github.io/</id>
  
  <author>
    <name>梧杵</name>
    
  </author>
  
  <generator uri="https://hexo.io/">Hexo</generator>
  
  <entry>
    <title>CSP并发模型</title>
    <link href="http://wuchuuuuu.github.io/2025/08/21/CSP%E5%B9%B6%E5%8F%91%E6%A8%A1%E5%9E%8B/"/>
    <id>http://wuchuuuuu.github.io/2025/08/21/CSP%E5%B9%B6%E5%8F%91%E6%A8%A1%E5%9E%8B/</id>
    <published>2025-08-21T12:08:41.000Z</published>
    <updated>2025-08-21T12:30:50.403Z</updated>
    
    <content type="html"><![CDATA[<p>Golang 的并发模型基于  CSP（Communicating Sequential Processes）  理论，这种并发模型通过 Goroutine 和 Channel 实现，强调通过通信来共享内存，而不是通过共享内存来通信。CSP 是一种由英国计算机科学家 Tony Hoare 在 1978 年提出的并发计算模型。以下是 Golang 中 CSP 并发模型的详细解释：</p><h3 id="1-CSP-概念"><a href="#1-CSP-概念" class="headerlink" title="1.  CSP 概念"></a>1.  CSP 概念</h3><ul><li>CSP 理论 ：CSP 关注的是并发进程之间的通信，而不是共享内存。这意味着不同的并发进程（或 Goroutine）之间通过发送和接收消息来进行交互，而不是通过共享变量。</li><li>进程 ：在 CSP 中，进程可以是执行某些任务的实体。在 Golang 中，进程的概念被 Goroutine 所取代。</li></ul><h3 id="2-Goroutine"><a href="#2-Goroutine" class="headerlink" title="2.  Goroutine"></a>2.  Goroutine</h3><ul><li>轻量级线程 ：Goroutine 是 Go 语言中的一种轻量级线程，它由 Go 运行时管理。每个 Goroutine 使用更少的内存和资源，启动和停止的开销也比操作系统线程小得多。</li><li>启动 Goroutine ：使用 <code>go</code> 关键字启动一个新的 Goroutine，代码示例：</li></ul>  <figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">go</span> <span class="function"><span class="keyword">func</span><span class="params">()</span></span> &#123;</span><br><span class="line">    fmt.Println(<span class="string">&quot;Hello, Goroutine!&quot;</span>)</span><br><span class="line">&#125;()</span><br></pre></td></tr></table></figure><p>  这段代码启动了一个新的 Goroutine 来执行匿名函数的内容。</p><ul><li>并发执行 ：Goroutine 是并发执行的，它们之间可以独立运行，并且 Go 运行时会在多个 Goroutine 之间调度。</li></ul><h3 id="3-Channel"><a href="#3-Channel" class="headerlink" title="3.  Channel"></a>3.  Channel</h3><ul><li>通信管道 ：Channel 是 Golang 提供的一种用于 Goroutine 之间通信的机制。Channel 是类型安全的管道，通过它可以发送和接收特定类型的数据。</li><li>Channel 的创建 ：使用 <code>make</code> 函数创建一个 Channel，代码示例：</li></ul>  <figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ch := <span class="built_in">make</span>(<span class="keyword">chan</span> <span class="type">int</span>)</span><br></pre></td></tr></table></figure><p>  这段代码创建了一个传递 <code>int</code> 类型数据的 Channel。</p><ul><li><p>发送和接收 ：</p></li><li><p>发送 ：使用 <code>&lt;-</code> 操作符将数据发送到 Channel，例如：<code>ch &lt;- 42</code>。</p></li><li><p>接收 ：使用 <code>&lt;-</code> 操作符从 Channel 接收数据，例如：<code>value := &lt;- ch</code>。</p></li><li><p>同步和阻塞 ：</p></li><li><p>在没有缓冲区的 Channel 上，发送和接收是同步的。当一个 Goroutine 向 Channel 发送数据时，它将被阻塞，直到另一个 Goroutine 从 Channel 接收数据。</p></li><li><p>缓冲 Channel 可以在发送时不阻塞，直到缓冲区满；接收时不阻塞，直到缓冲区为空。</p></li></ul><h3 id="4-CSP-模型的核心原则"><a href="#4-CSP-模型的核心原则" class="headerlink" title="4.  CSP 模型的核心原则"></a>4.  CSP 模型的核心原则</h3><ul><li>通过通信共享内存 ：在 Golang 中，数据在 Goroutine 之间传递时，通常通过 Channel 进行通信。这种模式避免了多 Goroutine 访问共享内存引发的竞争条件，从而减少了数据竞争和锁的使用。</li><li>同步机制 ：Channel 提供了天然的同步机制。当一个 Goroutine 发送数据到一个无缓冲的 Channel 时，它会阻塞，直到另一个 Goroutine 接收数据。这种机制避免了手动使用锁来同步数据。</li></ul><h3 id="5-select语句"><a href="#5-select语句" class="headerlink" title="5. select语句"></a>5. select语句</h3><ul><li>多路复用 ：<code>select</code> 语句允许一个 Goroutine 同时等待多个 Channel 操作。它的语法与 <code>switch</code> 类似，但每个 <code>case</code> 都是一个 Channel 操作。</li><li>非阻塞操作 ：<code>select</code> 可以用于实现非阻塞的发送、接收或超时控制。</li></ul><p>  代码示例：</p>  <figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">select</span> &#123;</span><br><span class="line"><span class="keyword">case</span> msg := &lt;-ch1:</span><br><span class="line">    fmt.Println(<span class="string">&quot;Received&quot;</span>, msg)</span><br><span class="line"><span class="keyword">case</span> ch2 &lt;- <span class="number">42</span>:</span><br><span class="line">    fmt.Println(<span class="string">&quot;Sent to ch2&quot;</span>)</span><br><span class="line"><span class="keyword">default</span>:</span><br><span class="line">    fmt.Println(<span class="string">&quot;No communication&quot;</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="6-CSP-并发模型的优点"><a href="#6-CSP-并发模型的优点" class="headerlink" title="6.  CSP 并发模型的优点"></a>6.  CSP 并发模型的优点</h3><ul><li>简化并发编程 ：通过 Goroutine 和 Channel，Go 语言使得并发编程更加直观，开发者不需要直接处理复杂的锁和条件变量。</li><li>安全性 ：CSP 模型降低了并发操作中数据竞争和死锁的风险。</li><li>可伸缩性 ：由于 Goroutine 是非常轻量级的，Go 的并发模型具有高可伸缩性，可以轻松创建数十万甚至更多的 Goroutine。</li></ul><h3 id="7-应用场景"><a href="#7-应用场景" class="headerlink" title="7.  应用场景"></a>7.  应用场景</h3><ul><li>任务并发执行 ：在 Web 服务器、微服务架构、并行计算等需要高并发的场景中，Goroutine 和 Channel 被广泛应用。</li><li>生产者-消费者模型 ：通过 Channel 实现数据的生产和消费，确保在高并发下的安全性和性能。</li></ul><h3 id="8-CSP-模型的限制"><a href="#8-CSP-模型的限制" class="headerlink" title="8.  CSP 模型的限制"></a>8.  CSP 模型的限制</h3><ul><li>不适用于共享内存密集的场景 ：在需要频繁访问和修改共享内存的场景中，CSP 模型可能不是最佳选择。</li><li>理解曲线 ：尽管 Go 语言的 CSP 模型使并发编程变得更简单，但对于初学者来说，理解 Channel 的阻塞、缓冲等机制仍有一定的学习曲线。</li></ul><h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><p>Golang 的 CSP 并发模型通过 Goroutine 和 Channel 实现了简洁且高效的并发编程方式。在这种模型中，Goroutine 是执行单元，而 Channel 是它们之间通信的桥梁，通过通信来共享内存，而不是通过共享内存来通信。这种模型不仅提高了程序的并发能力，还减少了编程中的复杂性。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;Golang 的并发模型基于  CSP（Communicating Sequential Processes）  理论，这种并发模型通过 Goroutine 和 Channel 实现，强调通过通信来共享内存，而不是通过共享内存来通信。CSP 是一种由英国计算机科学家 Ton</summary>
      
    
    
    
    <category term="GO八股文" scheme="http://wuchuuuuu.github.io/categories/GO%E5%85%AB%E8%82%A1%E6%96%87/"/>
    
    
    <category term="goroutine" scheme="http://wuchuuuuu.github.io/tags/goroutine/"/>
    
    <category term="channel" scheme="http://wuchuuuuu.github.io/tags/channel/"/>
    
  </entry>
  
  <entry>
    <title>Context的使用场景</title>
    <link href="http://wuchuuuuu.github.io/2025/08/21/Context%E7%9A%84%E4%BD%BF%E7%94%A8%E5%9C%BA%E6%99%AF/"/>
    <id>http://wuchuuuuu.github.io/2025/08/21/Context%E7%9A%84%E4%BD%BF%E7%94%A8%E5%9C%BA%E6%99%AF/</id>
    <published>2025-08-21T12:08:41.000Z</published>
    <updated>2025-08-21T12:30:50.402Z</updated>
    
    <content type="html"><![CDATA[<p>在 Go 语言中，<code>context</code> 包提供了一种在 goroutine 之间传递信号的方法，用于管理请求的生命周期和控制并发操作。<code>context</code> 主要用于以下几个场景：</p><h3 id="1-控制请求的生命周期"><a href="#1-控制请求的生命周期" class="headerlink" title="1.  控制请求的生命周期"></a>1.  控制请求的生命周期</h3><h4 id="场景描述"><a href="#场景描述" class="headerlink" title="场景描述"></a>场景描述</h4><ul><li>在处理 HTTP 请求时，通常需要确保请求处理过程中能够及时取消、超时或结束。这尤其重要当请求涉及多个下游服务调用时，若一个服务响应缓慢或失败，必须取消所有其他正在进行的操作。</li></ul><h4 id="使用示例"><a href="#使用示例" class="headerlink" title="使用示例"></a>使用示例</h4><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">handler</span><span class="params">(w http.ResponseWriter, r *http.Request)</span></span> &#123;</span><br><span class="line">    ctx := r.Context()</span><br><span class="line">    resultChan := <span class="built_in">make</span>(<span class="keyword">chan</span> <span class="type">string</span>)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">go</span> <span class="function"><span class="keyword">func</span><span class="params">()</span></span> &#123;</span><br><span class="line">        <span class="comment">// 模拟耗时操作</span></span><br><span class="line">        time.Sleep(<span class="number">2</span> * time.Second)</span><br><span class="line">        resultChan &lt;- <span class="string">&quot;result&quot;</span></span><br><span class="line">    &#125;()</span><br><span class="line"></span><br><span class="line">    <span class="keyword">select</span> &#123;</span><br><span class="line">    <span class="keyword">case</span> &lt;-ctx.Done():</span><br><span class="line">        <span class="comment">// 请求取消或超时</span></span><br><span class="line">        http.Error(w, <span class="string">&quot;request canceled&quot;</span>, http.StatusRequestTimeout)</span><br><span class="line">    <span class="keyword">case</span> result := &lt;-resultChan:</span><br><span class="line">        <span class="comment">// 正常返回结果</span></span><br><span class="line">        fmt.Fprintln(w, result)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ul><li>解释 ：在这个例子中，通过 <code>ctx.Done()</code> 来监听请求是否被取消或超时，从而决定是否终止操作。</li></ul><h3 id="2-处理超时和截止日期"><a href="#2-处理超时和截止日期" class="headerlink" title="2.  处理超时和截止日期"></a>2.  处理超时和截止日期</h3><h4 id="场景描述-1"><a href="#场景描述-1" class="headerlink" title="场景描述"></a>场景描述</h4><ul><li>当处理需要网络调用或长时间运行的操作时，设定一个超时时间或截止日期是很重要的。<code>context</code> 可以传递一个超时或截止日期，自动取消操作，避免资源的浪费。</li></ul><h4 id="使用示例-1"><a href="#使用示例-1" class="headerlink" title="使用示例"></a>使用示例</h4><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">fetchData</span><span class="params">(ctx context.Context)</span></span> (<span class="type">string</span>, <span class="type">error</span>) &#123;</span><br><span class="line">    ctx, cancel := context.WithTimeout(ctx, <span class="number">2</span>*time.Second)</span><br><span class="line">    <span class="keyword">defer</span> cancel()</span><br><span class="line"></span><br><span class="line">    ch := <span class="built_in">make</span>(<span class="keyword">chan</span> <span class="type">string</span>, <span class="number">1</span>)</span><br><span class="line">    <span class="keyword">go</span> <span class="function"><span class="keyword">func</span><span class="params">()</span></span> &#123;</span><br><span class="line">        <span class="comment">// 模拟耗时操作</span></span><br><span class="line">        time.Sleep(<span class="number">3</span> * time.Second)</span><br><span class="line">        ch &lt;- <span class="string">&quot;data&quot;</span></span><br><span class="line">    &#125;()</span><br><span class="line"></span><br><span class="line">    <span class="keyword">select</span> &#123;</span><br><span class="line">    <span class="keyword">case</span> &lt;-ctx.Done():</span><br><span class="line">        <span class="keyword">return</span> <span class="string">&quot;&quot;</span>, ctx.Err() <span class="comment">// 返回超时或取消错误</span></span><br><span class="line">    <span class="keyword">case</span> result := &lt;-ch:</span><br><span class="line">        <span class="keyword">return</span> result, <span class="literal">nil</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ul><li>解释 ：<code>context.WithTimeout</code> 创建了一个带有超时的 <code>context</code>，当操作超过指定时间后，<code>ctx.Done()</code> 会被触发。</li></ul><h3 id="3-传递元数据"><a href="#3-传递元数据" class="headerlink" title="3.  传递元数据"></a>3.  传递元数据</h3><h4 id="场景描述-2"><a href="#场景描述-2" class="headerlink" title="场景描述"></a>场景描述</h4><ul><li>在微服务架构中，可能需要在服务之间传递一些与请求相关的元数据，例如认证信息、跟踪 ID 等。<code>context</code> 提供了传递这些信息的方式。</li></ul><h4 id="使用示例-2"><a href="#使用示例-2" class="headerlink" title="使用示例"></a>使用示例</h4><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    ctx := context.Background()</span><br><span class="line">    ctx = context.WithValue(ctx, <span class="string">&quot;requestID&quot;</span>, <span class="string">&quot;12345&quot;</span>)</span><br><span class="line"></span><br><span class="line">    processRequest(ctx)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">processRequest</span><span class="params">(ctx context.Context)</span></span> &#123;</span><br><span class="line">    reqID := ctx.Value(<span class="string">&quot;requestID&quot;</span>)</span><br><span class="line">    fmt.Println(<span class="string">&quot;Request ID:&quot;</span>, reqID)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ul><li>解释 ：<code>context.WithValue</code> 可以将请求的元数据存储在 <code>context</code> 中，并在整个请求链中传递和访问。</li></ul><h3 id="4-协同工作"><a href="#4-协同工作" class="headerlink" title="4.  协同工作"></a>4.  协同工作</h3><h4 id="场景描述-3"><a href="#场景描述-3" class="headerlink" title="场景描述"></a>场景描述</h4><ul><li>在复杂的并发任务中，不同的 goroutine 可能需要相互协作，或需要在特定条件下取消其他 goroutine。<code>context</code> 可以用于协同工作，统一管理多个 goroutine 的状态。</li></ul><h4 id="使用示例-3"><a href="#使用示例-3" class="headerlink" title="使用示例"></a>使用示例</h4><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    ctx, cancel := context.WithCancel(context.Background())</span><br><span class="line"></span><br><span class="line">    <span class="keyword">go</span> worker(ctx, <span class="string">&quot;worker1&quot;</span>)</span><br><span class="line">    <span class="keyword">go</span> worker(ctx, <span class="string">&quot;worker2&quot;</span>)</span><br><span class="line"></span><br><span class="line">    time.Sleep(<span class="number">1</span> * time.Second)</span><br><span class="line">    cancel() <span class="comment">// 取消所有工作</span></span><br><span class="line"></span><br><span class="line">    time.Sleep(<span class="number">1</span> * time.Second)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">worker</span><span class="params">(ctx context.Context, name <span class="type">string</span>)</span></span> &#123;</span><br><span class="line">    <span class="keyword">for</span> &#123;</span><br><span class="line">        <span class="keyword">select</span> &#123;</span><br><span class="line">        <span class="keyword">case</span> &lt;-ctx.Done():</span><br><span class="line">            fmt.Println(name, <span class="string">&quot;stopped&quot;</span>)</span><br><span class="line">            <span class="keyword">return</span></span><br><span class="line">        <span class="keyword">default</span>:</span><br><span class="line">            fmt.Println(name, <span class="string">&quot;working&quot;</span>)</span><br><span class="line">            time.Sleep(<span class="number">500</span> * time.Millisecond)</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ul><li>解释 ：<code>context.WithCancel</code> 创建了一个可以手动取消的 <code>context</code>，通过 <code>cancel()</code> 函数可以取消所有与该 <code>context</code> 关联的操作。</li></ul><h3 id="5-限制并发数量"><a href="#5-限制并发数量" class="headerlink" title="5.  限制并发数量"></a>5.  限制并发数量</h3><h4 id="场景描述-4"><a href="#场景描述-4" class="headerlink" title="场景描述"></a>场景描述</h4><ul><li>在某些情况下，需要限制并发执行的 goroutine 的数量，避免过度消耗系统资源。<code>context</code> 可以与信号量或 <code>sync.WaitGroup</code> 一起使用来实现并发控制。</li></ul><h4 id="使用示例-4"><a href="#使用示例-4" class="headerlink" title="使用示例"></a>使用示例</h4><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    ctx, cancel := context.WithCancel(context.Background())</span><br><span class="line">    <span class="keyword">defer</span> cancel()</span><br><span class="line"></span><br><span class="line">    sem := <span class="built_in">make</span>(<span class="keyword">chan</span> <span class="keyword">struct</span>&#123;&#125;, <span class="number">3</span>) <span class="comment">// 限制并发数为3</span></span><br><span class="line">    <span class="keyword">var</span> wg sync.WaitGroup</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> i := <span class="number">0</span>; i &lt; <span class="number">10</span>; i++ &#123;</span><br><span class="line">        wg.Add(<span class="number">1</span>)</span><br><span class="line">        <span class="keyword">go</span> <span class="function"><span class="keyword">func</span><span class="params">(i <span class="type">int</span>)</span></span> &#123;</span><br><span class="line">            <span class="keyword">defer</span> wg.Done()</span><br><span class="line">            sem &lt;- <span class="keyword">struct</span>&#123;&#125;&#123;&#125; <span class="comment">// 获取信号</span></span><br><span class="line">            <span class="keyword">defer</span> <span class="function"><span class="keyword">func</span><span class="params">()</span></span> &#123; &lt;-sem &#125;() <span class="comment">// 释放信号</span></span><br><span class="line"></span><br><span class="line">            worker(ctx, i)</span><br><span class="line">        &#125;(i)</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    wg.Wait()</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">worker</span><span class="params">(ctx context.Context, id <span class="type">int</span>)</span></span> &#123;</span><br><span class="line">    <span class="keyword">select</span> &#123;</span><br><span class="line">    <span class="keyword">case</span> &lt;-ctx.Done():</span><br><span class="line">        fmt.Printf(<span class="string">&quot;worker %d canceled\n&quot;</span>, id)</span><br><span class="line">        <span class="keyword">return</span></span><br><span class="line">    <span class="keyword">default</span>:</span><br><span class="line">        fmt.Printf(<span class="string">&quot;worker %d working\n&quot;</span>, id)</span><br><span class="line">        time.Sleep(<span class="number">1</span> * time.Second)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ul><li>解释 ：在这个例子中，信号量用于限制同时运行的 goroutine 数量，而 <code>context</code> 用于在需要时取消所有工作。</li></ul><h3 id="6-总结"><a href="#6-总结" class="headerlink" title="6.  总结"></a>6.  总结</h3><p><code>context</code> 在 Go 语言中主要用于管理请求的生命周期、处理超时、传递元数据、协同工作和限制并发。它提供了一种简洁且强大的方式来管理复杂的并发操作，特别是在涉及多个 goroutine 时。通过合理使用 <code>context</code>，可以编写更健壮、更可控的并发程序。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;在 Go 语言中，&lt;code&gt;context&lt;/code&gt; 包提供了一种在 goroutine 之间传递信号的方法，用于管理请求的生命周期和控制并发操作。&lt;code&gt;context&lt;/code&gt; 主要用于以下几个场景：&lt;/p&gt;
&lt;h3 id=&quot;1-控制请求的生命周期&quot;&gt;&lt;a</summary>
      
    
    
    
    <category term="GO八股文" scheme="http://wuchuuuuu.github.io/categories/GO%E5%85%AB%E8%82%A1%E6%96%87/"/>
    
    
    <category term="context" scheme="http://wuchuuuuu.github.io/tags/context/"/>
    
    <category term="ctx" scheme="http://wuchuuuuu.github.io/tags/ctx/"/>
    
  </entry>
  
  <entry>
    <title>GC 是怎样监听你的应用的</title>
    <link href="http://wuchuuuuu.github.io/2025/08/21/GC%20%E6%98%AF%E6%80%8E%E6%A0%B7%E7%9B%91%E5%90%AC%E4%BD%A0%E7%9A%84%E5%BA%94%E7%94%A8%E7%9A%84/"/>
    <id>http://wuchuuuuu.github.io/2025/08/21/GC%20%E6%98%AF%E6%80%8E%E6%A0%B7%E7%9B%91%E5%90%AC%E4%BD%A0%E7%9A%84%E5%BA%94%E7%94%A8%E7%9A%84/</id>
    <published>2025-08-21T12:08:41.000Z</published>
    <updated>2025-08-21T12:30:50.397Z</updated>
    
    <content type="html"><![CDATA[<p>Go 语言的垃圾回收（Garbage Collection, GC）机制是自动内存管理的一部分，旨在自动回收不再使用的内存，以防止内存泄漏并提高内存使用效率。Go 的 GC 机制通过多种方式“监听”你的应用，从而决定何时启动垃圾回收、回收哪些对象的内存，以及如何尽可能减少对应用性能的影响。以下是 Go GC 如何监听和管理应用的详细解释：</p><h3 id="1-GC-的基本原理"><a href="#1-GC-的基本原理" class="headerlink" title="1.  GC 的基本原理"></a>1.  GC 的基本原理</h3><ul><li>Go 的垃圾回收器采用  三色标记-清除算法 。这个算法的主要步骤包括：<ol><li>标记阶段 ：GC 遍历从根对象（如全局变量、栈上的变量）可达的所有对象，并将这些对象标记为可达对象。</li><li>清除阶段 ：GC 清理未被标记的对象，将它们的内存释放。</li><li>并发标记 ：在标记过程中，应用程序仍然在运行，GC 和应用程序是并发执行的。</li></ol></li></ul><h3 id="2-GC-监听应用的方式"><a href="#2-GC-监听应用的方式" class="headerlink" title="2.  GC 监听应用的方式"></a>2.  GC 监听应用的方式</h3><h4 id="2-1-堆的增长"><a href="#2-1-堆的增长" class="headerlink" title="2.1  堆的增长"></a>2.1  堆的增长</h4><ul><li>Go 的 GC 主要关注堆内存的使用。当你的应用程序分配内存时，这些内存通常会分配在堆上。GC 通过跟踪堆的增长来“监听”你的应用。</li><li>当堆内存达到某个阈值时（由 <code>GOGC</code> 环境变量控制），GC 会自动触发一次垃圾回收周期。</li></ul><h4 id="2-2-内存分配"><a href="#2-2-内存分配" class="headerlink" title="2.2  内存分配"></a>2.2  内存分配</h4><ul><li>每当你的应用分配新对象时，GC 都会对内存分配进行监控。</li><li>Go 运行时为每个 Goroutine 分配一个小型对象池（<code>mcache</code>），这些对象池中分配的内存不会立即触发 GC。但当对象池耗尽或堆达到某个阈值时，GC 就会介入并检查是否需要进行垃圾回收。</li></ul><h4 id="2-3-堆对象的指针扫描"><a href="#2-3-堆对象的指针扫描" class="headerlink" title="2.3  堆对象的指针扫描"></a>2.3  堆对象的指针扫描</h4><ul><li>在标记阶段，GC 会扫描堆中的所有指针来识别可达的对象。</li><li>这个过程也可以看作是 GC 对应用程序状态的“监听”，因为它通过扫描指针来了解哪些对象仍在被引用，哪些对象已经不再使用。</li></ul><h4 id="2-4-协作式抢占"><a href="#2-4-协作式抢占" class="headerlink" title="2.4  协作式抢占"></a>2.4  协作式抢占</h4><ul><li>Go 的 GC 通过协作式抢占机制“监听”应用的执行。协作式抢占指的是 Goroutine 在执行期间，定期检查是否需要让出 CPU 以便让 GC 执行。</li><li>当 GC 需要进行标记或清理操作时，它会通知正在运行的 Goroutine，Goroutine 会在安全点（如函数调用、内存分配等）暂停执行，等待 GC 完成工作后再继续。</li></ul><h3 id="3-GC-调优"><a href="#3-GC-调优" class="headerlink" title="3.  GC 调优"></a>3.  GC 调优</h3><ul><li>GOGC 环境变量 ：<code>GOGC</code> 是一个控制 GC 频率的环境变量。它的默认值是 100，表示当堆内存增长到上一次 GC 后大小的 100% 时，触发下一次 GC。增大 <code>GOGC</code> 的值会减少 GC 的频率，适合内存较大且不频繁分配的场景；减小 <code>GOGC</code> 的值则会增加 GC 的频率，适合对内存占用敏感的应用。</li><li>内存限制 ：Go 1.19 及以后版本引入了 <code>GOMEMLIMIT</code> 选项，允许开发者设置一个硬内存上限。当应用内存接近这个上限时，GC 会更加积极地回收内存。</li></ul><h3 id="4-如何观察和调试-GC-行为"><a href="#4-如何观察和调试-GC-行为" class="headerlink" title="4.  如何观察和调试 GC 行为"></a>4.  如何观察和调试 GC 行为</h3><ul><li>运行时监控 ：Go 提供了多种工具来监控 GC 的行为，比如通过 <code>runtime.ReadMemStats</code> 函数获取内存分配和 GC 统计信息，或者使用 <code>pprof</code> 工具来分析内存分配和 GC 活动。</li><li>GC 日志 ：可以使用 <code>GODEBUG=gctrace=1</code> 环境变量来开启 GC 日志输出，帮助开发者分析 GC 的频率、暂停时间等详细信息。</li></ul><h3 id="5-GC-对应用的影响"><a href="#5-GC-对应用的影响" class="headerlink" title="5.  GC 对应用的影响"></a>5.  GC 对应用的影响</h3><ul><li>STW（Stop-the-World） ：尽管 Go 的 GC 设计成尽可能减少对应用程序的暂停，但在某些情况下（如堆扫描、最终清理阶段），仍然会出现短暂的 STW 停顿。Go 的 GC 不断优化，以减少这些停顿对应用性能的影响。</li><li>吞吐量与延迟的平衡 ：GC 在回收内存时可能会降低程序的吞吐量，而减少 GC 的频率则可能增加内存使用。通过调整 <code>GOGC</code> 等参数，开发者可以在内存使用和程序性能之间找到平衡点。</li></ul><p>Go 的 GC 通过监控堆内存、分配行为、指针扫描和协作式抢占等方式，智能地管理内存，并尽量减少对应用程序的干扰。了解这些机制可以帮助开发者优化 Go 应用程序的性能和内存使用。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;Go 语言的垃圾回收（Garbage Collection, GC）机制是自动内存管理的一部分，旨在自动回收不再使用的内存，以防止内存泄漏并提高内存使用效率。Go 的 GC 机制通过多种方式“监听”你的应用，从而决定何时启动垃圾回收、回收哪些对象的内存，以及如何尽可能减少对</summary>
      
    
    
    
    <category term="GO八股文" scheme="http://wuchuuuuu.github.io/categories/GO%E5%85%AB%E8%82%A1%E6%96%87/"/>
    
    
    <category term="gc" scheme="http://wuchuuuuu.github.io/tags/gc/"/>
    
    <category term="go" scheme="http://wuchuuuuu.github.io/tags/go/"/>
    
  </entry>
  
  <entry>
    <title>GC垃圾回收算法</title>
    <link href="http://wuchuuuuu.github.io/2025/08/21/GC%E5%9E%83%E5%9C%BE%E5%9B%9E%E6%94%B6%E7%AE%97%E6%B3%95/"/>
    <id>http://wuchuuuuu.github.io/2025/08/21/GC%E5%9E%83%E5%9C%BE%E5%9B%9E%E6%94%B6%E7%AE%97%E6%B3%95/</id>
    <published>2025-08-21T12:08:41.000Z</published>
    <updated>2025-08-21T12:30:50.397Z</updated>
    
    <content type="html"><![CDATA[<p>Go 语言的垃圾回收器（GC）是为了自动管理内存而设计的，它通过回收不再使用的内存来避免内存泄漏。Go 的垃圾回收器使用了多种算法，逐步演进以提高性能和减少对程序运行的影响。以下是 Go 语言垃圾回收器的主要算法和机制：</p><h3 id="1-标记-清除算法-Mark-and-Sweep"><a href="#1-标记-清除算法-Mark-and-Sweep" class="headerlink" title="1.  标记-清除算法 (Mark-and-Sweep)"></a>1.  标记-清除算法 (Mark-and-Sweep)</h3><h4 id="概念"><a href="#概念" class="headerlink" title="概念"></a>概念</h4><ul><li>标记-清除  是 Go 语言垃圾回收的核心算法之一。该算法分为两个阶段：标记（Mark）和清除（Sweep）。</li><li>标记阶段 ：遍历所有的对象，从根对象开始，标记所有可达的对象（即仍然在使用的对象）。</li><li>清除阶段 ：遍历堆中所有的对象，清除那些没有被标记为可达的对象，将它们的内存归还给系统。</li></ul><h4 id="过程"><a href="#过程" class="headerlink" title="过程"></a>过程</h4><ol><li>根集合的确定 ：根集合包括全局变量、当前在栈上的局部变量、以及 CPU 寄存器中的指针。</li><li>标记阶段 ：从根集合开始，递归地遍历所有可达对象，并将其标记为“存活”。</li><li>清除阶段 ：扫描整个堆内存，释放未被标记为“存活”的对象的内存。</li></ol><h3 id="2-三色标记算法-Tri-Color-Marking"><a href="#2-三色标记算法-Tri-Color-Marking" class="headerlink" title="2.  三色标记算法 (Tri-Color Marking)"></a>2.  三色标记算法 (Tri-Color Marking)</h3><h4 id="概念-1"><a href="#概念-1" class="headerlink" title="概念"></a>概念</h4><ul><li>三色标记算法  是对标记-清除算法的优化，解决了标记阶段与程序并发运行时的冲突。通过将对象分为三类（白色、灰色、黑色）来处理标记过程。</li></ul><h4 id="三色划分"><a href="#三色划分" class="headerlink" title="三色划分"></a>三色划分</h4><ul><li>白色 ：尚未访问过的对象。在标记阶段开始时，所有对象都是白色的。</li><li>灰色 ：已访问但其引用的对象还未标记的对象。</li><li>黑色 ：对象本身及其引用的所有对象都已访问并标记。</li></ul><h4 id="过程-1"><a href="#过程-1" class="headerlink" title="过程"></a>过程</h4><ol><li>初始状态 ：所有对象都是白色，根对象变成灰色。</li><li>标记阶段 ：从灰色对象开始，将它们引用的对象变成灰色，并将自身变成黑色，直到所有灰色对象都被处理完毕。</li><li>清除阶段 ：白色的对象就是未被访问到的对象，可以被安全地回收。</li></ol><h4 id="优点"><a href="#优点" class="headerlink" title="优点"></a>优点</h4><ul><li>该算法允许程序在标记阶段继续运行，通过将程序对内存的修改加入到标记过程中，避免了数据不一致的问题。</li></ul><h3 id="3-写屏障-Write-Barrier"><a href="#3-写屏障-Write-Barrier" class="headerlink" title="3.  写屏障 (Write Barrier)"></a>3.  写屏障 (Write Barrier)</h3><h4 id="概念-2"><a href="#概念-2" class="headerlink" title="概念"></a>概念</h4><ul><li>写屏障  是在垃圾回收过程中使用的一种机制，用来处理并发情况下的新对象创建和对象引用的改变。写屏障可以帮助保持三色标记算法的正确性。</li></ul><h4 id="实现"><a href="#实现" class="headerlink" title="实现"></a>实现</h4><ul><li>当程序对某个对象的引用发生变更时，写屏障会记录这种变更并确保被引用的对象状态被正确更新（例如，将被引用的白色对象立即变成灰色）。</li></ul><h4 id="作用"><a href="#作用" class="headerlink" title="作用"></a>作用</h4><ul><li>写屏障确保在垃圾回收进行时，即使有新的对象被创建或对象之间的引用发生变化，垃圾回收器也能够正确地标记这些对象。</li></ul><h3 id="4-增量式-GC-Incremental-GC"><a href="#4-增量式-GC-Incremental-GC" class="headerlink" title="4.  增量式 GC (Incremental GC)"></a>4.  增量式 GC (Incremental GC)</h3><h4 id="概念-3"><a href="#概念-3" class="headerlink" title="概念"></a>概念</h4><ul><li>增量式 GC  是一种将垃圾回收过程分成小块来执行的技术，目的是减少 Stop-the-World（STW）时间，使应用程序在垃圾回收时仍然能够继续执行。</li></ul><h4 id="过程-2"><a href="#过程-2" class="headerlink" title="过程"></a>过程</h4><ul><li>垃圾回收的标记阶段被分割成多个小的步骤，这些步骤可以与应用程序的执行交替进行，从而降低一次性 STW 的时间长度。</li></ul><h3 id="5-并发标记清除-Concurrent-Mark-and-Sweep"><a href="#5-并发标记清除-Concurrent-Mark-and-Sweep" class="headerlink" title="5.  并发标记清除 (Concurrent Mark-and-Sweep)"></a>5.  并发标记清除 (Concurrent Mark-and-Sweep)</h3><h4 id="概念-4"><a href="#概念-4" class="headerlink" title="概念"></a>概念</h4><ul><li>并发标记清除  是对传统标记-清除算法的进一步优化，允许标记阶段和应用程序并发执行，减少了垃圾回收对应用程序响应时间的影响。</li></ul><h4 id="过程-3"><a href="#过程-3" class="headerlink" title="过程"></a>过程</h4><ol><li>初始标记 ：STW，标记根对象。</li><li>并发标记 ：程序和垃圾回收器并发运行，标记堆中的存活对象。</li><li>重新标记 ：短暂的 STW，确保并发标记过程中未处理的对象被正确标记。</li><li>并发清除 ：并发清除未被标记的对象的内存。</li></ol><h3 id="6-压缩-Compaction"><a href="#6-压缩-Compaction" class="headerlink" title="6.  压缩 (Compaction)"></a>6.  压缩 (Compaction)</h3><h4 id="概念-5"><a href="#概念-5" class="headerlink" title="概念"></a>概念</h4><ul><li>压缩  是一种减少内存碎片的技术。在某些垃圾回收器中，清除阶段不仅回收了未使用的对象，还将存活对象移动到内存的连续区域，避免内存碎片化。</li><li>Golang 的垃圾回收器目前不主动进行内存压缩，但在特定场景下可能会通过内存分配策略减少碎片。</li></ul><h3 id="7-垃圾回收参数调优"><a href="#7-垃圾回收参数调优" class="headerlink" title="7.  垃圾回收参数调优"></a>7.  垃圾回收参数调优</h3><ul><li><code>GOGC</code> 环境变量 ：用于控制垃圾回收的频率，默认值为 100，表示堆内存每增长 100% 触发一次垃圾回收。增大该值可以减少垃圾回收频率，但会占用更多内存。</li><li><code>GODEBUG</code> 变量 ：可以通过设置 <code>GODEBUG=gctrace=1</code> 来开启 GC 日志，分析 GC 运行情况。</li></ul><h3 id="8-总结"><a href="#8-总结" class="headerlink" title="8.  总结"></a>8.  总结</h3><p>Go 的垃圾回收器通过标记-清除算法、三色标记、写屏障、增量式回收和并发标记清除等技术，确保了高效的内存管理，同时尽量减少了对应用程序运行的影响。通过合理的垃圾回收策略和调优，开发者可以在性能和内存使用之间找到平衡点。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;Go 语言的垃圾回收器（GC）是为了自动管理内存而设计的，它通过回收不再使用的内存来避免内存泄漏。Go 的垃圾回收器使用了多种算法，逐步演进以提高性能和减少对程序运行的影响。以下是 Go 语言垃圾回收器的主要算法和机制：&lt;/p&gt;
&lt;h3 id=&quot;1-标记-清除算法-Mark</summary>
      
    
    
    
    <category term="GO八股文" scheme="http://wuchuuuuu.github.io/categories/GO%E5%85%AB%E8%82%A1%E6%96%87/"/>
    
    
    <category term="gc" scheme="http://wuchuuuuu.github.io/tags/gc/"/>
    
    <category term="概念" scheme="http://wuchuuuuu.github.io/tags/%E6%A6%82%E5%BF%B5/"/>
    
  </entry>
  
  <entry>
    <title>GPM调度模型</title>
    <link href="http://wuchuuuuu.github.io/2025/08/21/GPM%E8%B0%83%E5%BA%A6%E6%A8%A1%E5%9E%8B/"/>
    <id>http://wuchuuuuu.github.io/2025/08/21/GPM%E8%B0%83%E5%BA%A6%E6%A8%A1%E5%9E%8B/</id>
    <published>2025-08-21T12:08:41.000Z</published>
    <updated>2025-08-21T12:30:50.402Z</updated>
    
    <content type="html"><![CDATA[<p>Go语言的GPM调度模型是Go运行时中用于处理并发的核心机制之一，它将Goroutine（轻量级线程）有效地映射到系统线程上，以最大化并发性能。GPM模型主要由三个部分组成：G（Goroutine）、P（Processor）、M（Machine）。让我们逐一详细介绍：</p><h3 id="1-G（Goroutine）"><a href="#1-G（Goroutine）" class="headerlink" title="1.  G（Goroutine）"></a>1.  G（Goroutine）</h3><ul><li>Goroutine  是Go语言中用于并发执行的轻量级线程，每个Goroutine都有自己的栈和上下文信息。</li><li>Goroutine相对于操作系统的线程更加轻量级，可以在同一时间内运行成千上万的Goroutine。</li></ul><h3 id="2-P（Processor）"><a href="#2-P（Processor）" class="headerlink" title="2.  P（Processor）"></a>2.  P（Processor）</h3><ul><li>P  是处理Goroutine的调度器的上下文，每个P包含一个本地运行队列（Local Run Queue），用于存储需要运行的Goroutine。</li><li>P的数量由<code>GOMAXPROCS</code>设置决定，它决定了并行执行的最大线程数。</li><li>P不仅管理Goroutine，还负责与M协作，将Goroutine分配给M执行。</li></ul><h3 id="3-M（Machine）"><a href="#3-M（Machine）" class="headerlink" title="3.  M（Machine）"></a>3.  M（Machine）</h3><ul><li>M  代表操作系统的线程，负责执行Goroutine。一个M一次只能执行一个Goroutine。</li><li>M是实际执行代码的工作单元，M与P绑定后才能执行Goroutine。</li><li>M可以通过调度器从全局运行队列中拉取新的Goroutine，也可以与其他M协作完成工作。</li></ul><h3 id="4-GPM模型的调度过程"><a href="#4-GPM模型的调度过程" class="headerlink" title="4.  GPM模型的调度过程"></a>4.  GPM模型的调度过程</h3><ul><li>调度器工作机制 ：Goroutine创建后会被放入P的本地队列，P会从该队列中选择Goroutine，并将其分配给M执行。如果本地队列为空，P可以从全局运行队列或其他P的队列中窃取任务。</li><li>工作窃取机制 ：如果一个P的本地队列为空，而另一个P的本地队列中有多个Goroutine，前者可以从后者中窃取任务，从而保持系统的高效利用率。</li><li>阻塞与调度 ：当M执行的Goroutine阻塞（例如I&#x2F;O操作）时，M会释放当前的P并等待P重新分配任务，从而避免资源浪费。</li></ul><h3 id="5-模型优点"><a href="#5-模型优点" class="headerlink" title="5.  模型优点"></a>5.  模型优点</h3><ul><li>高效的并发调度 ：GPM模型使得Go语言可以高效地管理数百万个Goroutine的并发执行。</li><li>可伸缩性 ：通过P与M的动态调度，GPM模型可以充分利用多核处理器的性能。</li><li>轻量级 ：Goroutine非常轻量，创建和切换的成本比系统线程要低得多。</li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;Go语言的GPM调度模型是Go运行时中用于处理并发的核心机制之一，它将Goroutine（轻量级线程）有效地映射到系统线程上，以最大化并发性能。GPM模型主要由三个部分组成：G（Goroutine）、P（Processor）、M（Machine）。让我们逐一详细介绍：&lt;/p</summary>
      
    
    
    
    <category term="GO八股文" scheme="http://wuchuuuuu.github.io/categories/GO%E5%85%AB%E8%82%A1%E6%96%87/"/>
    
    
    <category term="goroutine" scheme="http://wuchuuuuu.github.io/tags/goroutine/"/>
    
    <category term="gpm" scheme="http://wuchuuuuu.github.io/tags/gpm/"/>
    
  </entry>
  
  <entry>
    <title>Go协程的栈内存管理</title>
    <link href="http://wuchuuuuu.github.io/2025/08/21/Go%E5%8D%8F%E7%A8%8B%E7%9A%84%E6%A0%88%E5%86%85%E5%AD%98%E7%AE%A1%E7%90%86/"/>
    <id>http://wuchuuuuu.github.io/2025/08/21/Go%E5%8D%8F%E7%A8%8B%E7%9A%84%E6%A0%88%E5%86%85%E5%AD%98%E7%AE%A1%E7%90%86/</id>
    <published>2025-08-21T12:08:41.000Z</published>
    <updated>2025-08-21T12:30:50.398Z</updated>
    
    <content type="html"><![CDATA[<p>Go 协程（Goroutine）的栈内存管理是 Go 语言设计中一个重要的优化点。它通过灵活的栈增长策略，确保 Goroutine 能够高效地使用内存，支持高并发。以下是 Go 协程栈内存管理的详细介绍：</p><h3 id="1-栈的初始大小"><a href="#1-栈的初始大小" class="headerlink" title="1.  栈的初始大小"></a>1.  栈的初始大小</h3><ul><li>Goroutine 的栈在创建时非常小，通常只有 2KB 左右（不同的 Go 版本可能略有不同）。与传统线程的固定大小栈（通常是 1MB 或更多）相比，这种设计使得 Go 程序能够创建大量 Goroutine，而不占用大量内存。</li></ul><h3 id="2-栈的动态增长"><a href="#2-栈的动态增长" class="headerlink" title="2.  栈的动态增长"></a>2.  栈的动态增长</h3><ul><li>Go 协程的栈是动态增长的。当 Goroutine 运行时，如果当前栈空间不足以满足函数调用的需求，Go 运行时会自动将栈扩展到更大的空间。</li><li>栈增长的过程大致如下：<ol><li>检查栈空间 ：每次函数调用时，Go 运行时都会检查当前栈空间是否足够。</li><li>栈扩展 ：如果不够，运行时会分配一块更大的内存，将原栈中的内容拷贝到新栈，并更新相应的栈指针。</li><li>继续执行 ：栈扩展后，程序继续执行，几乎不会受到影响。</li></ol></li></ul><h3 id="3-栈的动态缩小"><a href="#3-栈的动态缩小" class="headerlink" title="3.  栈的动态缩小"></a>3.  栈的动态缩小</h3><ul><li>除了动态增长，Go 的栈还可以动态缩小。当 Goroutine 中的栈深度减少时，Go 运行时可能会将栈缩小，以释放不再使用的内存。</li><li>缩小栈的操作不像增长栈那样频繁，因为缩小操作可能会影响性能。但在一些长生命周期的 Goroutine 中，这种机制有助于释放未使用的内存。</li></ul><h3 id="4-栈分片和垃圾回收"><a href="#4-栈分片和垃圾回收" class="headerlink" title="4.  栈分片和垃圾回收"></a>4.  栈分片和垃圾回收</h3><ul><li>Goroutine 的栈由多个分片（segments）组成，随着栈的增长和缩小，这些分片可能被分配或释放。</li><li>Go 运行时的垃圾回收器（GC）会定期检查这些分片，并回收那些不再需要的内存。这种设计使得 Goroutine 的栈管理更加灵活，能够有效地控制内存使用。</li></ul><h3 id="5-栈溢出和保护"><a href="#5-栈溢出和保护" class="headerlink" title="5.  栈溢出和保护"></a>5.  栈溢出和保护</h3><ul><li>为了防止栈溢出，Go 运行时在栈的末尾保留了一块保护区（guard page）。如果 Goroutine 访问了这块保护区，运行时会触发栈溢出错误，防止非法内存访问。</li><li>当发生栈溢出时，程序会触发运行时异常，并输出相关的错误信息，帮助开发者定位问题。</li></ul><h3 id="6-栈管理的优点"><a href="#6-栈管理的优点" class="headerlink" title="6.  栈管理的优点"></a>6.  栈管理的优点</h3><ul><li>高效内存使用 ：通过动态调整栈大小，Go 能够在高并发场景下高效地利用内存，支持成千上万的 Goroutine。</li><li>自动化管理 ：开发者无需手动管理栈内存，Go 运行时会自动处理栈的增长和缩小，简化了并发编程的复杂性。</li><li>安全性 ：栈的动态增长和保护机制降低了栈溢出和内存非法访问的风险，提升了程序的健壮性。</li></ul><p>Go 的栈内存管理机制是其支持高并发的重要基础之一。通过灵活的栈调整策略，Go 语言能够在高并发场景下保持良好的性能和内存效率。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;Go 协程（Goroutine）的栈内存管理是 Go 语言设计中一个重要的优化点。它通过灵活的栈增长策略，确保 Goroutine 能够高效地使用内存，支持高并发。以下是 Go 协程栈内存管理的详细介绍：&lt;/p&gt;
&lt;h3 id=&quot;1-栈的初始大小&quot;&gt;&lt;a href=&quot;#1-</summary>
      
    
    
    
    <category term="GO八股文" scheme="http://wuchuuuuu.github.io/categories/GO%E5%85%AB%E8%82%A1%E6%96%87/"/>
    
    
    <category term="goroutine" scheme="http://wuchuuuuu.github.io/tags/goroutine/"/>
    
    <category term="go" scheme="http://wuchuuuuu.github.io/tags/go/"/>
    
  </entry>
  
  <entry>
    <title>HTTP Client大量长连接保持</title>
    <link href="http://wuchuuuuu.github.io/2025/08/21/HTTP%20Client%E5%A4%A7%E9%87%8F%E9%95%BF%E8%BF%9E%E6%8E%A5%E4%BF%9D%E6%8C%81/"/>
    <id>http://wuchuuuuu.github.io/2025/08/21/HTTP%20Client%E5%A4%A7%E9%87%8F%E9%95%BF%E8%BF%9E%E6%8E%A5%E4%BF%9D%E6%8C%81/</id>
    <published>2025-08-21T12:08:41.000Z</published>
    <updated>2025-08-21T12:30:50.395Z</updated>
    
    <content type="html"><![CDATA[<h3 id="问题起因"><a href="#问题起因" class="headerlink" title="问题起因"></a>问题起因</h3><p>在生产环境中，<code>server to server</code> 的服务出现大量 <code>TIME_WAIT</code> 状态的连接。通过 <code>netstat</code> 发现，连接不断建立但无法保持。抓取 TCP 包后确认，<code>request</code> 和 <code>response</code> 中的 <code>keepalive</code> 已正确设置，但每个 TCP 连接大约处理 6 次 HTTP 请求后就会关闭。</p><h3 id="排查分析"><a href="#排查分析" class="headerlink" title="排查分析"></a>排查分析</h3><p>在 Golang 中，<code>IdleConn</code> 的数量不仅受到 <code>MaxIdleConns</code> 的限制，还受到 <code>MaxIdleConnsPerHost</code> 的限制。</p><ul><li><code>MaxIdleConns</code> ：控制整个客户端的连接池中的最大空闲连接数。</li><li><code>MaxIdleConnsPerHost</code> ：控制每个主机的最大空闲连接数。</li></ul><p>在 <code>http.DefaultTransport</code> 中，这些参数默认没有显式配置，其中 <code>MaxIdleConnsPerHost</code> 的默认值是 2。这意味着每个主机最多只能保留 2 个空闲连接，即使你的客户端允许更多的空闲连接数（通过 <code>MaxIdleConns</code> 设置）。</p><p>因此，在高并发场景下，如果你没有调整 <code>MaxIdleConnsPerHost</code>，可能会导致性能瓶颈。为了提高性能，你可以通过自定义 <code>http.Transport</code> 来增加 <code>MaxIdleConnsPerHost</code> 的值，例如：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">transport := &amp;http.Transport&#123;</span><br><span class="line">    MaxIdleConns:       <span class="number">100</span>,  <span class="comment">// 设置整个连接池的最大空闲连接数</span></span><br><span class="line">    MaxIdleConnsPerHost: <span class="number">10</span>,  <span class="comment">// 设置每个主机的最大空闲连接数</span></span><br><span class="line">    <span class="comment">// 其他配置</span></span><br><span class="line">&#125;</span><br><span class="line">client := &amp;http.Client&#123;</span><br><span class="line">    Transport: transport,</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>通过这种配置，你可以更好地控制连接池行为，提升应用的并发性能。</p><h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><p>出现大量的 <code>TIME_WAIT</code> 状态以及频繁关闭连接的问题，通常与以下几个因素有关：</p><ol><li>连接管理配置问题 ：</li></ol><ul><li><code>TIME_WAIT</code> 是 TCP 协议中规定的一种状态，当一方主动关闭连接时，会进入 <code>TIME_WAIT</code> 状态，等待 2MSL（Maximum Segment Lifetime）时间，以确保对方接收到了最后的 ACK 确认。这段时间内，端口仍然被占用。</li><li>如果你的服务频繁建立和关闭连接，<code>TIME_WAIT</code> 状态的连接会大量增加，导致可用端口不足。</li></ul><ol start="2"><li><code>Keep-Alive</code> 配置问题 ：</li></ol><ul><li>虽然 <code>keepalive</code> 设置了，但如果服务端或客户端的连接池管理不当，可能仍会导致连接频繁关闭。</li><li>HTTP 的 <code>Keep-Alive</code> 机制可以维持 TCP 连接，但需要配合合理的连接池配置，确保连接不会过早关闭。</li></ul><ol start="3"><li>服务器负载和资源限制 ：</li></ol><ul><li>高负载情况下，服务器可能会主动关闭连接，尤其是在连接数超过设定阈值时。</li><li>可以检查系统资源（如文件描述符、网络缓冲区）是否足够。</li></ul><ol start="4"><li>配置参数调整 ：</li></ol><ul><li><code>tcp_tw_reuse</code> 和 <code>tcp_tw_recycle</code> 是 Linux 内核中的两个参数，可以调整 <code>TIME_WAIT</code> 状态的处理方式，但不建议在生产环境中随意更改，因为可能引入其他问题。</li><li>调整 <code>MaxIdleConns</code> 和 <code>MaxIdleConnsPerHost</code>，增加连接池的连接数，减少新建连接的频率。</li></ul><h4 id="解决方案建议"><a href="#解决方案建议" class="headerlink" title="解决方案建议"></a>解决方案建议</h4><ol><li>调整连接池配置 ：</li></ol><ul><li>确保 Golang 的客户端和服务端都配置了合理的连接池，避免频繁创建和销毁连接。</li><li>例如，客户端可以通过增加 <code>DefaultMaxIdleConnsPerHost</code> 或 <code>MaxIdleConnsPerHost</code> 参数来保持更多的空闲连接。</li></ul>   <figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">client := &amp;http.Client&#123;</span><br><span class="line">    Transport: &amp;http.Transport&#123;</span><br><span class="line">        MaxIdleConns:        <span class="number">100</span>,</span><br><span class="line">        MaxIdleConnsPerHost: <span class="number">100</span>,</span><br><span class="line">        IdleConnTimeout:     <span class="number">90</span> * time.Second,</span><br><span class="line">    &#125;,</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ol start="2"><li>使用长连接（Keep-Alive） ：</li></ol><ul><li>确保 HTTP 请求使用了 <code>Keep-Alive</code>，并且服务端正确地支持并配置了 <code>Keep-Alive</code>。</li><li>如果是 RESTful 服务，尽量减少短连接的使用。</li></ul><ol start="3"><li>调节服务器的资源配置 ：</li></ol><ul><li>检查服务器的 <code>ulimit</code> 设置，确保文件描述符足够多。</li><li>查看并优化服务器的网络配置，例如增大连接追踪表的大小，减少 <code>TIME_WAIT</code> 的持续时间。</li></ul><ol start="4"><li>负载均衡与反向代理 ：</li></ol><ul><li>如果服务部署在负载均衡器或反向代理之后，确保这些中间件的连接管理配置合理，以减少对后端服务的连接压力。</li></ul><ol start="5"><li>监控和日志分析 ：</li></ol><ul><li>通过监控工具和日志分析，确认哪些请求导致了连接的频繁关闭，并针对性地进行优化。</li></ul><h3 id="最后"><a href="#最后" class="headerlink" title="最后"></a>最后</h3><p>大量的 <code>TIME_WAIT</code> 状态通常表明连接管理不当或负载过高。通过合理配置连接池、优化服务器资源配置、并确保 <code>Keep-Alive</code> 正确使用，可以显著减少不必要的连接关闭，并降低 <code>TIME_WAIT</code> 的积累。持续监控系统表现并进行相应的调整，将有助于保持服务的稳定性和性能。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h3 id=&quot;问题起因&quot;&gt;&lt;a href=&quot;#问题起因&quot; class=&quot;headerlink&quot; title=&quot;问题起因&quot;&gt;&lt;/a&gt;问题起因&lt;/h3&gt;&lt;p&gt;在生产环境中，&lt;code&gt;server to server&lt;/code&gt; 的服务出现大量 &lt;code&gt;TIME_WAIT&lt;/</summary>
      
    
    
    
    <category term="GO八股文" scheme="http://wuchuuuuu.github.io/categories/GO%E5%85%AB%E8%82%A1%E6%96%87/"/>
    
    
    <category term="http" scheme="http://wuchuuuuu.github.io/tags/http/"/>
    
    <category term="maxidleconnsperhost" scheme="http://wuchuuuuu.github.io/tags/maxidleconnsperhost/"/>
    
  </entry>
  
  <entry>
    <title>Interface内部实现的理解</title>
    <link href="http://wuchuuuuu.github.io/2025/08/21/Interface%E5%86%85%E9%83%A8%E5%AE%9E%E7%8E%B0%E7%9A%84%E7%90%86%E8%A7%A3/"/>
    <id>http://wuchuuuuu.github.io/2025/08/21/Interface%E5%86%85%E9%83%A8%E5%AE%9E%E7%8E%B0%E7%9A%84%E7%90%86%E8%A7%A3/</id>
    <published>2025-08-21T12:08:41.000Z</published>
    <updated>2025-08-21T12:30:50.398Z</updated>
    
    <content type="html"><![CDATA[<p>在 Go 语言中，<code>interface</code> 是一种强大的抽象机制，用于定义一组方法签名，而不提供具体的实现。通过接口，Go 语言支持多态和依赖注入等面向对象的编程概念。了解接口的内部实现对于理解 Go 的类型系统和接口机制非常重要。</p><h3 id="1-接口的定义和使用"><a href="#1-接口的定义和使用" class="headerlink" title="1. 接口的定义和使用"></a>1. 接口的定义和使用</h3><h4 id="定义"><a href="#定义" class="headerlink" title="定义"></a>定义</h4><p>接口在 Go 中定义了一个方法集，这些方法没有具体的实现，具体的实现由类型提供。</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">type</span> Animal <span class="keyword">interface</span> &#123;</span><br><span class="line">    Speak() <span class="type">string</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="实现"><a href="#实现" class="headerlink" title="实现"></a>实现</h4><p>类型通过实现接口定义的方法集来满足接口。</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">type</span> Dog <span class="keyword">struct</span>&#123;&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(d Dog)</span></span> Speak() <span class="type">string</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="string">&quot;Woof&quot;</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">type</span> Cat <span class="keyword">struct</span>&#123;&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(c Cat)</span></span> Speak() <span class="type">string</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="string">&quot;Meow&quot;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="2-接口的内部结构"><a href="#2-接口的内部结构" class="headerlink" title="2. 接口的内部结构"></a>2. 接口的内部结构</h3><p>在 Go 的运行时系统中，接口的实现涉及以下几个关键数据结构：</p><h4 id="接口类型（interface）"><a href="#接口类型（interface）" class="headerlink" title="接口类型（interface）"></a>接口类型（interface）</h4><p>接口在 Go 中通常由两个部分组成：</p><ul><li>类型信息 （Type）：存储接口的具体类型。</li><li>值信息 （Value）：存储实现接口的具体值。</li></ul><p>Go 运行时将接口的数据结构定义为一个结构体：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">type</span> iface <span class="keyword">struct</span> &#123;</span><br><span class="line">    typ *rtype   <span class="comment">// 类型信息</span></span><br><span class="line">    value unsafe.Pointer <span class="comment">// 值信息</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ul><li><code>typ</code> ：指向类型描述符的指针，用于确定实际的实现类型。</li><li><code>value</code> ：指向实际值的指针，存储具体的数据。</li></ul><h4 id="类型描述符（rtype）"><a href="#类型描述符（rtype）" class="headerlink" title="类型描述符（rtype）"></a>类型描述符（rtype）</h4><p><code>rtype</code> 是 Go 运行时中用来描述类型的结构体，包含了关于类型的所有信息，包括方法集、类型大小等。</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">type</span> rtype <span class="keyword">struct</span> &#123;</span><br><span class="line">    size        <span class="type">uintptr</span></span><br><span class="line">    ptrdata     <span class="type">uintptr</span></span><br><span class="line">    hash        <span class="type">uint32</span></span><br><span class="line">    tflag       tflag</span><br><span class="line">    align       <span class="type">uint8</span></span><br><span class="line">    fieldAlign  <span class="type">uint8</span></span><br><span class="line">    kind        <span class="type">uint8</span></span><br><span class="line">    alg         *typeAlg</span><br><span class="line">    gcdata      *<span class="type">byte</span></span><br><span class="line">    str         nameOff</span><br><span class="line">    ptrToThis   typeOff</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ul><li><code>size</code> ：类型的大小。</li><li><code>ptrdata</code> ：指针数据，用于垃圾回收。</li><li><code>hash</code> ：类型的哈希值。</li><li><code>kind</code> ：类型的类别（如结构体、切片等）。</li><li><code>alg</code> ：类型的算法（如比较、拷贝等）。</li></ul><h4 id="方法表"><a href="#方法表" class="headerlink" title="方法表"></a>方法表</h4><p>接口的具体实现依赖于方法表（Method Table）。方法表是一个存储类型方法实现的表格，用于接口调用时的动态绑定。</p><h3 id="3-接口实现的工作流程"><a href="#3-接口实现的工作流程" class="headerlink" title="3. 接口实现的工作流程"></a>3. 接口实现的工作流程</h3><ol><li>类型和方法注册 ：</li></ol><ul><li>在编译时，Go 编译器会生成类型的 <code>rtype</code> 结构体，并在方法表中注册该类型实现的接口方法。</li></ul><ol start="2"><li>接口赋值 ：</li></ol><ul><li>当一个值赋给接口时，Go 运行时系统会创建一个 <code>iface</code> 结构体，其中 <code>typ</code> 指向实际类型的 <code>rtype</code>，<code>value</code> 指向实际值。</li></ul><ol start="3"><li>接口方法调用 ：</li></ol><ul><li>当调用接口的方法时，运行时会根据 <code>iface</code> 中的 <code>typ</code> 信息从方法表中找到具体的方法实现，并通过 <code>value</code> 指针执行实际的方法。</li></ul><h3 id="4-接口的零值和类型断言"><a href="#4-接口的零值和类型断言" class="headerlink" title="4. 接口的零值和类型断言"></a>4. 接口的零值和类型断言</h3><h4 id="接口零值"><a href="#接口零值" class="headerlink" title="接口零值"></a>接口零值</h4><ul><li>零值 ：接口的零值是 <code>nil</code>，即 <code>iface</code> 的 <code>typ</code> 和 <code>value</code> 都为 <code>nil</code>。一个 <code>nil</code> 接口值不持有任何数据，也没有方法可调用。</li></ul><h4 id="类型断言"><a href="#类型断言" class="headerlink" title="类型断言"></a>类型断言</h4><ul><li>类型断言 ：用于将接口值转换为具体类型。它通过 <code>iface</code> 的 <code>typ</code> 信息来检查接口的实际类型，并进行类型转换。</li></ul><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">var</span> a Animal = Dog&#123;&#125;</span><br><span class="line">d, ok := a.(Dog)</span><br><span class="line"><span class="keyword">if</span> ok &#123;</span><br><span class="line">    fmt.Println(d.Speak())</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="5-性能优化"><a href="#5-性能优化" class="headerlink" title="5. 性能优化"></a>5. 性能优化</h3><ul><li>接口值的存储 ：接口的值存储为指针，可以避免在接口赋值时进行大量的数据复制，提高性能。</li><li>方法表的缓存 ：接口方法的动态调用通过方法表实现，可以避免每次调用都进行反射操作，提高调用效率。</li></ul><h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><p>Go 的接口机制通过内部的 <code>iface</code> 结构体和 <code>rtype</code> 结构体实现了高效的类型抽象和多态。理解接口的内部实现可以帮助更好地优化 Go 程序的性能，并深入理解 Go 的类型系统和运行时行为。接口的实现涉及类型信息和方法表的动态绑定，使得接口的调用具有灵活性和高效性。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;在 Go 语言中，&lt;code&gt;interface&lt;/code&gt; 是一种强大的抽象机制，用于定义一组方法签名，而不提供具体的实现。通过接口，Go 语言支持多态和依赖注入等面向对象的编程概念。了解接口的内部实现对于理解 Go 的类型系统和接口机制非常重要。&lt;/p&gt;
&lt;h3 id</summary>
      
    
    
    
    <category term="GO八股文" scheme="http://wuchuuuuu.github.io/categories/GO%E5%85%AB%E8%82%A1%E6%96%87/"/>
    
    
    <category term="go" scheme="http://wuchuuuuu.github.io/tags/go/"/>
    
    <category term="rtype" scheme="http://wuchuuuuu.github.io/tags/rtype/"/>
    
  </entry>
  
  <entry>
    <title>Stop the World (STW)</title>
    <link href="http://wuchuuuuu.github.io/2025/08/21/Stop%20the%20World%20(STW)/"/>
    <id>http://wuchuuuuu.github.io/2025/08/21/Stop%20the%20World%20(STW)/</id>
    <published>2025-08-21T12:08:41.000Z</published>
    <updated>2025-08-21T12:30:50.399Z</updated>
    
    <content type="html"><![CDATA[<p> Stop-the-World (STW)  是垃圾回收（GC）过程中的一个重要概念，指的是在垃圾回收期间，程序的所有 Goroutine 会被暂停（或“停止”），以保证在回收内存时的一致性和正确性。在 Go 语言的垃圾回收过程中，STW 主要用于标记和清除阶段。</p><h3 id="1-STW-的必要性"><a href="#1-STW-的必要性" class="headerlink" title="1.  STW 的必要性"></a>1.  STW 的必要性</h3><p>STW 的主要目的是确保在进行垃圾回收时，所有的内存引用（即指针）都是一致的。由于垃圾回收过程中需要遍历堆内存和指针，STW 可以防止以下问题：</p><ul><li>数据竞争 ：在 GC 的过程中，若内存被修改，可能会导致对对象的错误引用或丢失。</li><li>一致性问题 ：垃圾回收需要在程序处于一致状态时进行，STW 可以确保在标记和清除阶段内存的完整性。</li></ul><h3 id="2-GC-中的-STW-阶段"><a href="#2-GC-中的-STW-阶段" class="headerlink" title="2.  GC 中的 STW 阶段"></a>2.  GC 中的 STW 阶段</h3><p>在 Go 语言的垃圾回收过程中，STW 通常会发生在以下几个阶段：</p><ol><li>标记阶段 ：GC 必须遍历所有可达对象的引用，标记这些对象。为了保证遍历的一致性，所有的 Goroutine 必须暂停，直到标记阶段完成。</li><li>清理阶段 ：GC 清除那些没有被标记的对象。这一阶段同样需要 STW，以确保删除的内存不再被任何 Goroutine 使用。</li></ol><h3 id="3-STW-的影响"><a href="#3-STW-的影响" class="headerlink" title="3.  STW 的影响"></a>3.  STW 的影响</h3><ul><li>暂停时间 ：STW 会导致应用程序在垃圾回收期间暂停。虽然 Go 的垃圾回收器在设计时尽量减少 STW 的时间，但在大规模堆内存和高并发场景下，STW 的暂停时间可能会影响应用程序的响应性和吞吐量。</li><li>用户体验 ：对于实时性要求高的应用程序，STW 的暂停可能会导致延迟增加，从而影响用户体验。</li></ul><h3 id="4-Go-语言中的-STW-优化"><a href="#4-Go-语言中的-STW-优化" class="headerlink" title="4.  Go 语言中的 STW 优化"></a>4.  Go 语言中的 STW 优化</h3><p>Go 语言的垃圾回收器在设计时考虑了 STW 的影响，并进行了优化，以减少其对应用程序的干扰：</p><ul><li>并发垃圾回收 ：Go 的垃圾回收器采用并发标记算法，即使在垃圾回收的过程中，应用程序仍然可以运行。标记阶段被分为多个阶段，其中一些可以与应用程序的执行并发进行。</li><li>增量式回收 ：通过将垃圾回收过程分为多个小步骤，Go 语言能够减少每个 STW 期间的时间，从而降低对应用程序的暂停时间。</li><li>GC 速度调节 ：<code>GOGC</code> 环境变量允许开发者调节 GC 的频率。增大 <code>GOGC</code> 的值可以减少 GC 的频率，从而减少 STW 的总次数，但可能会导致更高的内存使用。</li></ul><h3 id="5-如何监控和调试-STW"><a href="#5-如何监控和调试-STW" class="headerlink" title="5.  如何监控和调试 STW"></a>5.  如何监控和调试 STW</h3><ul><li><code>runtime.ReadMemStats</code> ：可以通过 <code>runtime.ReadMemStats</code> 函数获取垃圾回收的统计信息，包括 STW 的时间。</li><li>GC 日志 ：使用 <code>GODEBUG=gctrace=1</code> 环境变量可以开启 GC 日志，帮助分析 GC 的频率和 STW 的时间。</li><li>性能分析工具 ：Go 提供了 <code>pprof</code> 工具，可以用来分析内存分配和 GC 活动，帮助识别和优化 GC 相关的性能问题。</li></ul><h3 id="6-总结"><a href="#6-总结" class="headerlink" title="6.  总结"></a>6.  总结</h3><p>Stop-the-World (STW) 是垃圾回收中不可避免的一部分，用于保证回收过程中的内存一致性。虽然 STW 会导致程序暂停，影响应用程序的实时性，但 Go 语言通过并发标记、增量回收和优化调节等手段来减轻 STW 的影响。理解 STW 及其优化方法可以帮助开发者更好地设计和优化 Go 应用程序，尤其是在高并发和大规模应用中。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt; Stop-the-World (STW)  是垃圾回收（GC）过程中的一个重要概念，指的是在垃圾回收期间，程序的所有 Goroutine 会被暂停（或“停止”），以保证在回收内存时的一致性和正确性。在 Go 语言的垃圾回收过程中，STW 主要用于标记和清除阶段。&lt;/p&gt;
</summary>
      
    
    
    
    <category term="GO八股文" scheme="http://wuchuuuuu.github.io/categories/GO%E5%85%AB%E8%82%A1%E6%96%87/"/>
    
    
    <category term="gc" scheme="http://wuchuuuuu.github.io/tags/gc/"/>
    
    <category term="stw" scheme="http://wuchuuuuu.github.io/tags/stw/"/>
    
  </entry>
  
  <entry>
    <title>chan底层原理</title>
    <link href="http://wuchuuuuu.github.io/2025/08/21/chan%E5%BA%95%E5%B1%82%E5%8E%9F%E7%90%86/"/>
    <id>http://wuchuuuuu.github.io/2025/08/21/chan%E5%BA%95%E5%B1%82%E5%8E%9F%E7%90%86/</id>
    <published>2025-08-21T12:08:41.000Z</published>
    <updated>2025-08-21T12:30:50.407Z</updated>
    
    <content type="html"><![CDATA[<p><code>chan</code> 是 Go 语言中的一种用于 Goroutine 之间通信的原语，它提供了 Goroutine 之间的同步和数据传递机制。<code>chan</code> 的底层实现涉及队列、锁、信号量以及 Goroutine 的调度等内容。以下是 <code>chan</code> 的底层原理的详细解释：</p><h3 id="1-基本结构"><a href="#1-基本结构" class="headerlink" title="1.  基本结构"></a>1.  基本结构</h3><ul><li>chan 数据结构 ：在 Go 语言的源码中，<code>chan</code> 的数据结构被定义为一个 <code>hchan</code> 结构体，主要包含以下内容：</li><li>buf ：缓冲区指针，用于存储通道内的数据（仅当缓冲区大小大于 0 时存在）。</li><li>qcount ：通道中当前的元素数量。</li><li>dataqsiz ：缓冲区大小，即通道的容量。</li><li>sendx ：发送索引，指示下一个值应写入缓冲区的位置。</li><li>recvx ：接收索引，指示下一个值应从缓冲区中读取的位置。</li><li>recvq ：一个等待接收数据的 Goroutine 队列（FIFO）。</li><li>sendq ：一个等待发送数据的 Goroutine 队列（FIFO）。</li><li>lock ：互斥锁，用于保护 <code>chan</code> 的并发访问。</li></ul><h3 id="2-发送（Send）操作"><a href="#2-发送（Send）操作" class="headerlink" title="2.  发送（Send）操作"></a>2.  发送（Send）操作</h3><ul><li>当一个 Goroutine 执行发送操作时：<ol><li>检查通道状态 ：如果通道已关闭，发送操作会引发 panic。</li><li>尝试直接发送 ：如果通道有可用的缓冲区空间，数据直接写入缓冲区。</li><li>等待接收 ：如果通道没有缓冲区空间，且没有等待接收的 Goroutine，当前发送 Goroutine 将被阻塞，并排队等待接收者。</li><li>唤醒接收者 ：如果有等待接收的 Goroutine，发送操作会直接将数据发送给接收者，并唤醒接收者。</li></ol></li></ul><h3 id="3-接收（Receive）操作"><a href="#3-接收（Receive）操作" class="headerlink" title="3.  接收（Receive）操作"></a>3.  接收（Receive）操作</h3><ul><li>当一个 Goroutine 执行接收操作时：<ol><li>检查通道状态 ：如果通道已关闭且缓冲区为空，接收操作会立即返回一个零值和一个标识通道已关闭的标志。</li><li>尝试直接接收 ：如果通道中有数据可供接收，数据直接从缓冲区中读取。</li><li>等待发送者 ：如果缓冲区为空且没有等待发送的 Goroutine，当前接收 Goroutine 将被阻塞，并排队等待发送者。</li><li>唤醒发送者 ：如果有等待发送的 Goroutine，接收操作会直接从发送者那里获取数据，并唤醒发送者。</li></ol></li></ul><h3 id="4-缓冲通道-vs-非缓冲通道"><a href="#4-缓冲通道-vs-非缓冲通道" class="headerlink" title="4.  缓冲通道 vs. 非缓冲通道"></a>4.  缓冲通道 vs. 非缓冲通道</h3><ul><li>非缓冲通道 ：没有缓冲区，发送和接收操作必须完全同步。发送者必须等待接收者，反之亦然。</li><li>缓冲通道 ：具有缓冲区，发送操作可以在缓冲区未满时立即完成，而无需等待接收者；同样，接收操作可以在缓冲区非空时立即完成，而无需等待发送者。</li></ul><h3 id="5-阻塞与唤醒机制"><a href="#5-阻塞与唤醒机制" class="headerlink" title="5.  阻塞与唤醒机制"></a>5.  阻塞与唤醒机制</h3><ul><li>当一个 Goroutine 在 <code>chan</code> 上被阻塞时，它会被放入相应的 <code>sendq</code> 或 <code>recvq</code> 队列中。</li><li>队列是一个 FIFO 队列，确保按照顺序唤醒等待的 Goroutine。</li><li>通过 <code>select</code> 语句，可以在多个通道上等待，并以非确定性的方式选择一个可用的通道进行操作。</li></ul><h3 id="6-关闭通道"><a href="#6-关闭通道" class="headerlink" title="6.  关闭通道"></a>6.  关闭通道</h3><ul><li>关闭通道是由 <code>close()</code> 函数完成的。关闭通道后，所有阻塞的接收操作将立即返回一个零值和一个 <code>ok == false</code> 的标志。</li><li>关闭通道后，不能再向该通道发送数据，试图发送数据将导致 panic。</li></ul><h3 id="7-性能优化"><a href="#7-性能优化" class="headerlink" title="7.  性能优化"></a>7.  性能优化</h3><ul><li>Go 运行时对 <code>chan</code> 的操作进行了多种优化，例如：<ul><li>无锁快路径 ：在没有竞争的情况下，发送和接收操作可以在无锁的快路径上执行，以减少锁争用带来的性能开销。</li><li>批量处理 ：在某些情况下，<code>chan</code> 会尝试批量移动数据以提高性能。</li></ul></li></ul><p><code>chan</code> 是 Go 语言并发模型的核心部分，它通过通道和 Goroutine 实现了 CSP（Communicating Sequential Processes）模型。理解 <code>chan</code> 的底层实现有助于编写高效并发程序并更好地调试和优化 Go 代码。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;&lt;code&gt;chan&lt;/code&gt; 是 Go 语言中的一种用于 Goroutine 之间通信的原语，它提供了 Goroutine 之间的同步和数据传递机制。&lt;code&gt;chan&lt;/code&gt; 的底层实现涉及队列、锁、信号量以及 Goroutine 的调度等内容。以下是 &lt;c</summary>
      
    
    
    
    <category term="GO八股文" scheme="http://wuchuuuuu.github.io/categories/GO%E5%85%AB%E8%82%A1%E6%96%87/"/>
    
    
    <category term="goroutine" scheme="http://wuchuuuuu.github.io/tags/goroutine/"/>
    
    <category term="chan" scheme="http://wuchuuuuu.github.io/tags/chan/"/>
    
  </entry>
  
  <entry>
    <title>dlv分析golang进程</title>
    <link href="http://wuchuuuuu.github.io/2025/08/21/dlv%E5%88%86%E6%9E%90golang%E8%BF%9B%E7%A8%8B/"/>
    <id>http://wuchuuuuu.github.io/2025/08/21/dlv%E5%88%86%E6%9E%90golang%E8%BF%9B%E7%A8%8B/</id>
    <published>2025-08-21T12:08:41.000Z</published>
    <updated>2025-08-21T12:30:50.401Z</updated>
    
    <content type="html"><![CDATA[<p>使用 <code>dlv</code>（Delve）分析 Golang 进程的 CPU 占用高问题可以帮助开发人员深入理解代码运行时的行为，找出性能瓶颈或问题。<code>dlv</code> 是 Go 语言中的调试器，可以用来调试、分析运行中的进程。</p><h3 id="使用-dlv-分析-Go-进程-CPU-占用高问题的步骤"><a href="#使用-dlv-分析-Go-进程-CPU-占用高问题的步骤" class="headerlink" title="使用 dlv 分析 Go 进程 CPU 占用高问题的步骤"></a>使用 dlv 分析 Go 进程 CPU 占用高问题的步骤</h3><h4 id="1-确认-CPU-占用高的进程"><a href="#1-确认-CPU-占用高的进程" class="headerlink" title="1.  确认 CPU 占用高的进程"></a>1.  确认 CPU 占用高的进程</h4><p>首先，你需要找到 CPU 占用高的进程。在 Linux 环境下，可以使用 <code>top</code> 或 <code>htop</code> 命令来查看系统中各个进程的 CPU 使用情况。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">top</span><br></pre></td></tr></table></figure><p>通过 <code>top</code>，你可以找到 Go 程序的进程 ID（PID）。</p><h4 id="2-使用-dlv-附加到运行中的进程"><a href="#2-使用-dlv-附加到运行中的进程" class="headerlink" title="2.  使用 dlv 附加到运行中的进程"></a>2.  使用 dlv 附加到运行中的进程</h4><p>当找到占用 CPU 高的进程后，可以通过 <code>dlv</code> 附加到正在运行的 Go 进程。假设目标进程的 PID 为 <code>12345</code>：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">dlv attach 12345</span><br></pre></td></tr></table></figure><p><code>dlv attach</code> 会暂停该进程并进入调试模式。进入调试模式后，你可以查看进程的运行状态、查看当前的 Goroutine、函数调用栈等信息。</p><h4 id="3-查看-Goroutine"><a href="#3-查看-Goroutine" class="headerlink" title="3.  查看 Goroutine"></a>3.  查看 Goroutine</h4><p>使用 <code>dlv</code>，你可以查看当前所有的 Goroutine，检查是否有 Goroutine 堆积或死锁。运行以下命令来查看所有 Goroutine 的状态：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">goroutines</span><br></pre></td></tr></table></figure><p>你会看到当前所有 Goroutine 的列表，带有每个 Goroutine 的 ID 和执行中的函数信息。如果某些 Goroutine 在某个点停滞不前或有大量 Goroutine 堆积，这可能是导致高 CPU 使用的原因。</p><h4 id="4-检查-Goroutine-的堆栈"><a href="#4-检查-Goroutine-的堆栈" class="headerlink" title="4.  检查 Goroutine 的堆栈"></a>4.  检查 Goroutine 的堆栈</h4><p>对于特定的 Goroutine，可以进一步查看其调用栈，确认它正在执行的代码。可以通过以下命令查看某个 Goroutine 的调用栈：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">goroutine &lt;Goroutine_ID&gt; stack</span><br></pre></td></tr></table></figure><p>例如：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">goroutine 1 stack</span><br></pre></td></tr></table></figure><p>这会显示该 Goroutine 的调用栈，可以帮助你找出哪部分代码可能导致高 CPU 占用。</p><h4 id="5-查看当前执行的位置"><a href="#5-查看当前执行的位置" class="headerlink" title="5.  查看当前执行的位置"></a>5.  查看当前执行的位置</h4><p>你还可以检查当前进程正在执行的代码，查看 CPU 消耗的具体位置。通过 <code>break</code> 设置断点，在特定的代码行停止执行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">break</span> main.go:20</span><br></pre></td></tr></table></figure><p>然后使用 <code>continue</code> 命令继续执行程序，程序将在达到该断点时暂停，这样可以了解程序在特定代码路径上的运行情况。</p><h4 id="6-分析-CPU-Profiling"><a href="#6-分析-CPU-Profiling" class="headerlink" title="6.  分析 CPU Profiling"></a>6.  分析 CPU Profiling</h4><p>对于更深层次的 CPU 占用问题分析，可以利用 <code>pprof</code> 工具生成 CPU 性能报告。使用 <code>runtime/pprof</code> 包，可以通过代码生成 CPU profile 文件，并使用 <code>go tool pprof</code> 分析。</p><p>首先，在程序中添加以下代码来记录 CPU profile：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> (</span><br><span class="line">    <span class="string">&quot;os&quot;</span></span><br><span class="line">    <span class="string">&quot;runtime/pprof&quot;</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    f, _ := os.Create(<span class="string">&quot;cpu.prof&quot;</span>)</span><br><span class="line">    pprof.StartCPUProfile(f)</span><br><span class="line">    <span class="keyword">defer</span> pprof.StopCPUProfile()</span><br><span class="line">    <span class="comment">// your code</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>运行程序后，使用 <code>go tool pprof</code> 来分析 <code>cpu.prof</code>：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">go tool pprof cpu.prof</span><br></pre></td></tr></table></figure><p>进入 <code>pprof</code> 后，可以使用 <code>top</code>、<code>list</code> 等命令来查看哪些函数占用了最多的 CPU 时间。</p><h4 id="7-通过-trace-分析更多细节"><a href="#7-通过-trace-分析更多细节" class="headerlink" title="7.  通过 trace 分析更多细节"></a>7.  通过 trace 分析更多细节</h4><p>除了 <code>dlv</code> 和 <code>pprof</code>，还可以使用 Go 提供的 <code>trace</code> 工具来捕获更多的运行时信息。首先，修改代码以捕获 trace：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> (</span><br><span class="line">    <span class="string">&quot;os&quot;</span></span><br><span class="line">    <span class="string">&quot;runtime/trace&quot;</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    f, _ := os.Create(<span class="string">&quot;trace.out&quot;</span>)</span><br><span class="line">    trace.Start(f)</span><br><span class="line">    <span class="keyword">defer</span> trace.Stop()</span><br><span class="line">    <span class="comment">// your code</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>然后使用 <code>go tool trace</code> 查看 trace 文件：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">go tool trace trace.out</span><br></pre></td></tr></table></figure><p><code>trace</code> 工具能展示更多的调度、Goroutine 切换、GC 事件等，帮助更全面地分析程序性能。</p><h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><p>使用 <code>dlv</code> 调试 Go 进程 CPU 占用高的问题，可以从以下几方面着手：</p><ol><li>确认高 CPU 使用的进程 ，使用 <code>top</code> 查找进程。</li><li>附加到进程 ，使用 <code>dlv attach</code> 进入调试模式。</li><li>查看 Goroutine 状态 ，检查是否有 Goroutine 堆积或执行异常。</li><li>分析调用栈 ，定位消耗 CPU 资源的代码路径。</li><li>使用 <code>pprof</code> 和 <code>trace</code> 进行更详细的性能分析。</li></ol><p>通过这些步骤，可以有效找到并解决 Go 应用中的性能瓶颈问题。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;使用 &lt;code&gt;dlv&lt;/code&gt;（Delve）分析 Golang 进程的 CPU 占用高问题可以帮助开发人员深入理解代码运行时的行为，找出性能瓶颈或问题。&lt;code&gt;dlv&lt;/code&gt; 是 Go 语言中的调试器，可以用来调试、分析运行中的进程。&lt;/p&gt;
&lt;h3 id</summary>
      
    
    
    
    <category term="GO八股文" scheme="http://wuchuuuuu.github.io/categories/GO%E5%85%AB%E8%82%A1%E6%96%87/"/>
    
    
    <category term="goroutine" scheme="http://wuchuuuuu.github.io/tags/goroutine/"/>
    
    <category term="cpu" scheme="http://wuchuuuuu.github.io/tags/cpu/"/>
    
  </entry>
  
  <entry>
    <title>go runtime 简析</title>
    <link href="http://wuchuuuuu.github.io/2025/08/21/go%20runtime%20%E7%AE%80%E6%9E%90/"/>
    <id>http://wuchuuuuu.github.io/2025/08/21/go%20runtime%20%E7%AE%80%E6%9E%90/</id>
    <published>2025-08-21T12:08:41.000Z</published>
    <updated>2025-08-21T12:30:50.408Z</updated>
    
    <content type="html"><![CDATA[<p>Go 的运行时系统（runtime）是 Go 语言的核心组成部分之一，负责管理 Goroutine 的调度、垃圾回收、内存分配、系统调用等底层操作。它在 Go 程序的执行过程中扮演着重要角色。以下是对 Go 运行时系统的简要分析：</p><h3 id="1-Goroutine-调度"><a href="#1-Goroutine-调度" class="headerlink" title="1. Goroutine 调度"></a>1. Goroutine 调度</h3><h4 id="Goroutine-管理"><a href="#Goroutine-管理" class="headerlink" title="Goroutine 管理"></a>Goroutine 管理</h4><ul><li>Goroutine  是 Go 的轻量级线程，由 Go 运行时系统管理。</li><li>运行时系统负责 Goroutine 的创建、调度、挂起和恢复。</li></ul><h4 id="调度模型"><a href="#调度模型" class="headerlink" title="调度模型"></a>调度模型</h4><ul><li><p>G-P-M 模型 ：</p></li><li><p>G (Goroutine) ：表示正在执行的 Goroutine。</p></li><li><p>P (Processor) ：表示调度器的逻辑处理单元，负责管理 Goroutine 的调度。</p></li><li><p>M (Machine) ：表示操作系统线程，运行 Goroutine。</p></li><li><p>调度器 ：负责将 Goroutine 分配到可用的线程（M）上，并管理它们的状态（就绪、挂起、运行等）。</p></li><li><p>Goroutine 调度的关键函数 ：</p></li><li><p><code>gopark</code> ：将 Goroutine 挂起，进入等待状态。</p></li><li><p><code>goready</code> ：将挂起的 Goroutine 恢复为可运行状态。</p></li></ul><h3 id="2-内存管理"><a href="#2-内存管理" class="headerlink" title="2. 内存管理"></a>2. 内存管理</h3><h4 id="内存分配"><a href="#内存分配" class="headerlink" title="内存分配"></a>内存分配</h4><ul><li><code>malloc</code> 和 <code>free</code> ：Go 运行时系统提供了自己的内存分配器，用于处理内存分配和释放操作，优化了内存管理的效率。</li><li>内存分配器 ：基于分配器（如  <code>heap</code>  和  <code>stack</code> ）来分配和回收内存，处理不同大小的内存块。</li></ul><h4 id="垃圾回收（GC）"><a href="#垃圾回收（GC）" class="headerlink" title="垃圾回收（GC）"></a>垃圾回收（GC）</h4><ul><li>GC 算法 ：Go 使用并发垃圾回收算法来管理内存。</li><li>标记-清除算法 ：通过标记活动对象和清除未被标记的对象来回收内存。</li><li>STW（Stop-the-World） ：在 GC 过程中，可能会发生 STW 事件，这时候所有 Goroutine 会被挂起，直到垃圾回收完成。</li></ul><h3 id="3-系统调用"><a href="#3-系统调用" class="headerlink" title="3. 系统调用"></a>3. 系统调用</h3><h4 id="系统调用的封装"><a href="#系统调用的封装" class="headerlink" title="系统调用的封装"></a>系统调用的封装</h4><ul><li>系统调用 ：Go 运行时提供了对操作系统系统调用的封装，例如文件 I&#x2F;O、网络操作等。</li><li>跨平台性 ：运行时系统通过不同的实现方式（如  <code>syscall</code>  包）在不同平台上提供一致的接口。</li></ul><h3 id="4-运行时调度"><a href="#4-运行时调度" class="headerlink" title="4. 运行时调度"></a>4. 运行时调度</h3><h4 id="调度机制"><a href="#调度机制" class="headerlink" title="调度机制"></a>调度机制</h4><ul><li>抢占式调度 ：Go 运行时系统使用抢占式调度机制，以确保 Goroutine 在执行时不会阻塞其他 Goroutine 的执行。</li><li>工作窃取（Work Stealing） ：调度器使用工作窃取算法来平衡负载，将 Goroutine 从繁忙的线程迁移到空闲线程上。</li></ul><h3 id="5-运行时内部结构"><a href="#5-运行时内部结构" class="headerlink" title="5. 运行时内部结构"></a>5. 运行时内部结构</h3><h4 id="数据结构"><a href="#数据结构" class="headerlink" title="数据结构"></a>数据结构</h4><ul><li>Goroutine 栈 ：每个 Goroutine 都有一个栈，栈的大小可以动态调整。</li><li>调度队列 ：用于管理待执行和挂起的 Goroutine。</li></ul><h4 id="锁和同步"><a href="#锁和同步" class="headerlink" title="锁和同步"></a>锁和同步</h4><ul><li>锁机制 ：运行时系统内部使用各种锁（如  <code>mutex</code>  和  <code>spinlock</code> ）来管理 Goroutine 的状态和同步。</li><li>信号量 ：用于实现同步和阻塞机制，例如在 IO 操作中等待事件完成。</li></ul><h3 id="6-性能优化"><a href="#6-性能优化" class="headerlink" title="6. 性能优化"></a>6. 性能优化</h3><h4 id="自旋锁"><a href="#自旋锁" class="headerlink" title="自旋锁"></a>自旋锁</h4><ul><li>自旋锁 ：用于减少锁的竞争和上下文切换的开销，提高 Goroutine 的调度效率。</li></ul><h4 id="内存分配优化"><a href="#内存分配优化" class="headerlink" title="内存分配优化"></a>内存分配优化</h4><ul><li>内存池 ：运行时系统使用内存池来优化内存分配，减少分配和释放内存的开销。</li></ul><h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><p>Go 的运行时系统是 Go 语言的核心部分，负责 Goroutine 的调度、内存管理、系统调用的封装等任务。通过高效的调度机制、并发垃圾回收、内存优化等技术，Go 的运行时系统能够提供高效的性能和良好的用户体验。了解运行时的内部机制有助于更好地优化 Go 应用程序的性能，理解 Goroutine 的行为和内存管理策略。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;Go 的运行时系统（runtime）是 Go 语言的核心组成部分之一，负责管理 Goroutine 的调度、垃圾回收、内存分配、系统调用等底层操作。它在 Go 程序的执行过程中扮演着重要角色。以下是对 Go 运行时系统的简要分析：&lt;/p&gt;
&lt;h3 id=&quot;1-Gorouti</summary>
      
    
    
    
    <category term="GO八股文" scheme="http://wuchuuuuu.github.io/categories/GO%E5%85%AB%E8%82%A1%E6%96%87/"/>
    
    
    <category term="goroutine" scheme="http://wuchuuuuu.github.io/tags/goroutine/"/>
    
    <category term="go" scheme="http://wuchuuuuu.github.io/tags/go/"/>
    
  </entry>
  
  <entry>
    <title>gin路由树</title>
    <link href="http://wuchuuuuu.github.io/2025/08/21/gin%E8%B7%AF%E7%94%B1%E6%A0%91/"/>
    <id>http://wuchuuuuu.github.io/2025/08/21/gin%E8%B7%AF%E7%94%B1%E6%A0%91/</id>
    <published>2025-08-21T12:08:41.000Z</published>
    <updated>2025-08-21T12:30:50.395Z</updated>
    
    <content type="html"><![CDATA[<p>Gin 的路由树采用了一种高效的 前缀树（Trie Tree） 数据结构，用来快速匹配请求路径。Gin 的路由设计能够快速找到对应的路由处理函数，这种设计方式在高并发和高性能场景下表现非常优秀。</p><h3 id="Gin-路由树原理"><a href="#Gin-路由树原理" class="headerlink" title="Gin 路由树原理"></a>Gin 路由树原理</h3><p>Gin 使用的路由树基于前缀树的思想，每一个节点代表路径的一部分，路由按照不同的路径段逐级插入树中。树的节点有三种类型：</p><ol><li>静态节点 ：对应静态的 URL 路径，例如 <code>/user/profile</code>，这些路径段是固定的。</li><li>参数节点 ：对应 URL 中的动态参数段，用 <code>:</code> 标记，例如 <code>/user/:id</code>，其中 <code>:id</code> 是一个参数节点，匹配任意路径段。</li><li>通配符节点 ：使用 <code>*</code> 来匹配剩余的路径部分，例如 <code>/assets/*filepath</code>，匹配所有路径。</li></ol><h3 id="路由树的匹配过程"><a href="#路由树的匹配过程" class="headerlink" title="路由树的匹配过程"></a>路由树的匹配过程</h3><p>当收到一个 HTTP 请求时，Gin 的路由器会将请求路径分段，并根据每个路径段在路由树中逐级查找匹配的节点。Gin 优先匹配静态节点，其次匹配参数节点，最后匹配通配符节点。</p><h3 id="例子"><a href="#例子" class="headerlink" title="例子"></a>例子</h3><p>假设你在 Gin 中定义了以下几个路由：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">router := gin.Default()</span><br><span class="line"></span><br><span class="line">router.GET(<span class="string">&quot;/user/profile&quot;</span>, handlerProfile)</span><br><span class="line">router.GET(<span class="string">&quot;/user/:id&quot;</span>, handlerUser)</span><br><span class="line">router.GET(<span class="string">&quot;/assets/*filepath&quot;</span>, handlerAssets)</span><br></pre></td></tr></table></figure><p>在这个例子中，Gin 的路由树大致结构如下：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">        /</span><br><span class="line">        |</span><br><span class="line">      user</span><br><span class="line">      /   \</span><br><span class="line">profile   :id</span><br><span class="line">           |</span><br><span class="line">      assets/*filepath</span><br></pre></td></tr></table></figure><ol><li><code>/user/profile</code> 是一个静态路径，会直接匹配到 <code>handlerProfile</code>。</li><li><code>/user/123</code> 会匹配到 <code>/user/:id</code>，其中 <code>:id</code> 是参数节点，会将 <code>123</code> 作为参数传递给 <code>handlerUser</code>。</li><li><code>/assets/images/logo.png</code> 会匹配到 <code>/assets/*filepath</code>，<code>filepath</code> 参数的值将会是 <code>images/logo.png</code>。</li></ol><h3 id="路由树的优点"><a href="#路由树的优点" class="headerlink" title="路由树的优点"></a>路由树的优点</h3><ol><li>快速匹配 ：Gin 的路由树结构基于前缀树，通过逐级查找来匹配路径段，具有很高的查找效率。</li><li>低内存占用 ：路由树的结构通过共享公共前缀路径，可以大大减少内存的占用，尤其是在有大量相似路径的场景下。</li><li>灵活性 ：支持静态路由、参数路由和通配符路由，满足多种路由需求。</li></ol><h3 id="Gin-路由树的源码分析"><a href="#Gin-路由树的源码分析" class="headerlink" title="Gin 路由树的源码分析"></a>Gin 路由树的源码分析</h3><p>Gin 的路由树由 <code>radix tree</code> 实现，核心数据结构为 <code>node</code>，其中主要字段包括：</p><ul><li><code>path</code>：当前节点的路径部分。</li><li><code>wildChild</code>：是否为通配符节点。</li><li><code>nType</code>：节点类型，可能为静态、参数、通配符节点。</li><li><code>indices</code>：子节点的索引。</li><li><code>children</code>：子节点数组。</li></ul><h3 id="Golang-代码示例"><a href="#Golang-代码示例" class="headerlink" title="Golang 代码示例"></a>Golang 代码示例</h3><p>以下是一个简单的 Gin 路由定义示例，并展示了如何匹配静态路径、参数路径和通配符路径。</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> (</span><br><span class="line">    <span class="string">&quot;github.com/gin-gonic/gin&quot;</span></span><br><span class="line">    <span class="string">&quot;net/http&quot;</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    router := gin.Default()</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 静态路由</span></span><br><span class="line">    router.GET(<span class="string">&quot;/user/profile&quot;</span>, <span class="function"><span class="keyword">func</span><span class="params">(c *gin.Context)</span></span> &#123;</span><br><span class="line">        c.String(http.StatusOK, <span class="string">&quot;User Profile&quot;</span>)</span><br><span class="line">    &#125;)</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 带参数的路由</span></span><br><span class="line">    router.GET(<span class="string">&quot;/user/:id&quot;</span>, <span class="function"><span class="keyword">func</span><span class="params">(c *gin.Context)</span></span> &#123;</span><br><span class="line">        id := c.Param(<span class="string">&quot;id&quot;</span>)</span><br><span class="line">        c.String(http.StatusOK, <span class="string">&quot;User ID: %s&quot;</span>, id)</span><br><span class="line">    &#125;)</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 通配符路由</span></span><br><span class="line">    router.GET(<span class="string">&quot;/assets/*filepath&quot;</span>, <span class="function"><span class="keyword">func</span><span class="params">(c *gin.Context)</span></span> &#123;</span><br><span class="line">        filepath := c.Param(<span class="string">&quot;filepath&quot;</span>)</span><br><span class="line">        c.String(http.StatusOK, <span class="string">&quot;Filepath: %s&quot;</span>, filepath)</span><br><span class="line">    &#125;)</span><br><span class="line"></span><br><span class="line">    router.Run(<span class="string">&quot;:8080&quot;</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ul><li>访问 <code>/user/profile</code>，返回 <code>&quot;User Profile&quot;</code>。</li><li>访问 <code>/user/123</code>，返回 <code>&quot;User ID: 123&quot;</code>。</li><li>访问 <code>/assets/images/logo.png</code>，返回 <code>&quot;Filepath: images/logo.png&quot;</code>。</li></ul><p>通过这种路由树结构，Gin 可以快速、灵活地匹配各种复杂的路由规则，确保路由的性能和灵活性。</p><h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><p>Gin 的路由树通过使用前缀树来高效地组织和查找路由规则，支持静态路由、参数路由和通配符路由。它在处理大规模路由时表现出色，能够保持较低的内存占用并且能够快速匹配请求路径。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;Gin 的路由树采用了一种高效的 前缀树（Trie Tree） 数据结构，用来快速匹配请求路径。Gin 的路由设计能够快速找到对应的路由处理函数，这种设计方式在高并发和高性能场景下表现非常优秀。&lt;/p&gt;
&lt;h3 id=&quot;Gin-路由树原理&quot;&gt;&lt;a href=&quot;#Gin-路由</summary>
      
    
    
    
    <category term="GO八股文" scheme="http://wuchuuuuu.github.io/categories/GO%E5%85%AB%E8%82%A1%E6%96%87/"/>
    
    
    <category term="gin" scheme="http://wuchuuuuu.github.io/tags/gin/"/>
    
    <category term="user" scheme="http://wuchuuuuu.github.io/tags/user/"/>
    
  </entry>
  
  <entry>
    <title>gopark函数和goready函数原理分析</title>
    <link href="http://wuchuuuuu.github.io/2025/08/21/gopark%E5%87%BD%E6%95%B0%E5%92%8Cgoready%E5%87%BD%E6%95%B0%E5%8E%9F%E7%90%86%E5%88%86%E6%9E%90/"/>
    <id>http://wuchuuuuu.github.io/2025/08/21/gopark%E5%87%BD%E6%95%B0%E5%92%8Cgoready%E5%87%BD%E6%95%B0%E5%8E%9F%E7%90%86%E5%88%86%E6%9E%90/</id>
    <published>2025-08-21T12:08:41.000Z</published>
    <updated>2025-08-21T12:30:50.399Z</updated>
    
    <content type="html"><![CDATA[<p>在 Golang 的运行时系统中，<code>gopark</code> 和 <code>goready</code> 是两个关键的函数，它们在 Goroutine 的调度和状态管理中起着重要作用。这两个函数在 Go 的调度模型中用于管理 Goroutine 的挂起（park）和恢复（ready）操作。</p><h3 id="1-gopark-函数"><a href="#1-gopark-函数" class="headerlink" title="1. gopark 函数"></a>1. gopark 函数</h3><h4 id="作用"><a href="#作用" class="headerlink" title="作用"></a>作用</h4><p><code>gopark</code> 函数用于挂起当前的 Goroutine，使其进入等待状态，直到它被唤醒。这个操作通常发生在 Goroutine 需要等待某些条件或事件时，比如等待 IO 完成、锁的释放等。</p><h4 id="工作流程"><a href="#工作流程" class="headerlink" title="工作流程"></a>工作流程</h4><ol><li>挂起 Goroutine ：</li></ol><ul><li><code>gopark</code> 会将当前 Goroutine 标记为需要挂起，并将其添加到等待队列中。</li><li>它会通过调用调度器的相关操作，将当前 Goroutine 状态设置为等待状态。</li></ul><ol start="2"><li>切换到其他 Goroutine ：</li></ol><ul><li>一旦 Goroutine 被标记为挂起，调度器会选择其他可运行的 Goroutine 来执行，从而使 CPU 能够继续处理其他任务。</li></ul><ol start="3"><li>挂起条件 ：</li></ol><ul><li>Goroutine 被挂起时，它会在特定条件被满足（如 IO 完成、锁被释放等）之前一直保持挂起状态。</li></ul><ol start="4"><li>唤醒 Goroutine ：</li></ol><ul><li>被挂起的 Goroutine 会在条件满足时被唤醒，并重新进入可运行状态。</li></ul><h4 id="代码示例"><a href="#代码示例" class="headerlink" title="代码示例"></a>代码示例</h4><p>在 Go 的运行时中，<code>gopark</code> 的实现涉及底层的调度和同步机制，但在用户层面，我们通常不直接调用这个函数。以下是一个简化的描述：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">gopark</span><span class="params">(reason <span class="type">string</span>, lock *runtime.Mutex, seq <span class="type">uintptr</span>, traceskip <span class="type">int</span>)</span></span> &#123;</span><br><span class="line">    <span class="comment">// 标记当前 Goroutine 需要挂起</span></span><br><span class="line">    runtime.goparkunlock(lock)</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 将当前 Goroutine 添加到等待队列</span></span><br><span class="line">    <span class="comment">// 当前 Goroutine 进入等待状态，直到被唤醒</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 调度器切换到其他 Goroutine</span></span><br><span class="line">    runtime.schedule()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="2-goready-函数"><a href="#2-goready-函数" class="headerlink" title="2. goready 函数"></a>2. goready 函数</h3><h4 id="作用-1"><a href="#作用-1" class="headerlink" title="作用"></a>作用</h4><p><code>goready</code> 函数用于将一个之前被挂起的 Goroutine 恢复为可运行状态，使其能够再次参与调度并被执行。它通常在某些事件发生时调用，例如 IO 操作完成、锁被释放等。</p><h4 id="工作流程-1"><a href="#工作流程-1" class="headerlink" title="工作流程"></a>工作流程</h4><ol><li>标记 Goroutine 为就绪 ：</li></ol><ul><li><code>goready</code> 会将指定的 Goroutine 标记为可运行状态，并将其从等待队列中移除。</li></ul><ol start="2"><li>调度 Goroutine ：</li></ol><ul><li>一旦 Goroutine 被标记为就绪，它会被加入到调度队列中，调度器会选择它来执行。</li></ul><ol start="3"><li>激活条件 ：</li></ol><ul><li><code>goready</code> 通常在某个条件满足时调用，例如当 IO 操作完成时，相关的 Goroutine 会被恢复，以继续处理数据。</li></ul><h4 id="代码示例-1"><a href="#代码示例-1" class="headerlink" title="代码示例"></a>代码示例</h4><p>在 Go 的运行时中，<code>goready</code> 的实现涉及底层的调度和同步机制，具体实现通常较复杂。以下是一个简化的描述：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">goready</span><span class="params">(g *g, reason <span class="type">string</span>)</span></span> &#123;</span><br><span class="line">    <span class="comment">// 将 Goroutine 从等待队列中移除</span></span><br><span class="line">    <span class="comment">// 标记 Goroutine 为就绪状态</span></span><br><span class="line">    g.status = Gwaiting</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 将 Goroutine 添加到调度队列中</span></span><br><span class="line">    runtime.schedule()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="3-gopark-和-goready-的核心原理分析"><a href="#3-gopark-和-goready-的核心原理分析" class="headerlink" title="3.   gopark 和 goready 的核心原理分析"></a>3.   gopark 和 goready 的核心原理分析</h3><h4 id="调度机制"><a href="#调度机制" class="headerlink" title="调度机制"></a>调度机制</h4><ul><li><code>gopark</code>  和  <code>goready</code>  是 Go 运行时调度机制的核心部分，用于管理 Goroutine 的挂起和恢复。</li><li><code>gopark</code>  会使当前 Goroutine 进入挂起状态，并在满足特定条件之前不会恢复执行。</li><li><code>goready</code>  会将之前挂起的 Goroutine 标记为可运行，并将其添加到调度队列中，以便调度器可以在合适的时间恢复其执行。</li></ul><h4 id="性能优化"><a href="#性能优化" class="headerlink" title="性能优化"></a>性能优化</h4><ul><li>自旋锁和信号量 ：Go 的调度器利用自旋锁和信号量来优化挂起和恢复操作，减少上下文切换的开销。</li><li>调度器 ：Go 运行时的调度器负责管理 Goroutine 的状态，确保系统资源的高效利用，并减少延迟。</li></ul><h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><p><code>gopark</code> 和 <code>goready</code> 是 Go 运行时中关键的调度函数，用于管理 Goroutine 的挂起和恢复。它们通过底层的调度机制和同步工具，确保 Goroutine 在等待条件满足时能够正确挂起，并在条件满足后高效地恢复执行。了解这些原理有助于更好地理解 Go 的调度模型和性能优化。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;在 Golang 的运行时系统中，&lt;code&gt;gopark&lt;/code&gt; 和 &lt;code&gt;goready&lt;/code&gt; 是两个关键的函数，它们在 Goroutine 的调度和状态管理中起着重要作用。这两个函数在 Go 的调度模型中用于管理 Goroutine 的挂起（par</summary>
      
    
    
    
    <category term="GO八股文" scheme="http://wuchuuuuu.github.io/categories/GO%E5%85%AB%E8%82%A1%E6%96%87/"/>
    
    
    <category term="goroutine" scheme="http://wuchuuuuu.github.io/tags/goroutine/"/>
    
    <category term="goready" scheme="http://wuchuuuuu.github.io/tags/goready/"/>
    
  </entry>
  
  <entry>
    <title>gofunc过程</title>
    <link href="http://wuchuuuuu.github.io/2025/08/21/gofunc%E8%BF%87%E7%A8%8B/"/>
    <id>http://wuchuuuuu.github.io/2025/08/21/gofunc%E8%BF%87%E7%A8%8B/</id>
    <published>2025-08-21T12:08:41.000Z</published>
    <updated>2025-08-21T12:30:50.400Z</updated>
    
    <content type="html"><![CDATA[<p><code>go func()</code> 创建一个 goroutine 的执行流程，具体过程如下：</p><ol><li><p>创建 goroutine ：通过 <code>go func()</code> 来创建一个新的 goroutine。</p></li><li><p>队列存储 ：有两个存储 G（goroutine）的队列，一个是 P 的本地队列，一个是全局 G 队列。新创建的 G 会首先保存在 P 的本地队列中，如果本地队列满了，那么 G 会存入全局队列。</p></li><li><p>G 的执行调度 ：G 需要 M（操作系统线程）来执行。每个 M 必须绑定一个 P，M 和 P 是 1:1 的关系。M 会从 P 的本地队列弹出一个可执行状态的 G 来执行。如果 P 的本地队列为空，M 会从其他 M-P 组合中偷取 G，或者从全局队列中获取 G 来执行。</p></li><li><p>M 调度 G 的执行 ：M 调度 G 的执行是一个循环机制，M 会不断地调度 G，直到所有 G 被执行。</p></li><li><p>系统调用的处理 ：当 M 执行 G 时，如果 G 发生了阻塞操作（例如发起系统调用），M 也会被阻塞。此时，如果其他 G 正在等待执行，runtime 会将这个 M 从 P 上解绑，并将 M 放入空闲线程池中，启动一个新的 M 来继续处理剩余的 G。如果有空闲的 M，则复用已有的 M 来执行 G。</p></li><li><p>M 系统调用结束后的恢复 ：当系统调用结束后，runtime 会尝试让原来的 G 和 M 重新获得一个 P 并继续执行。如果成功，M 和 G 重新进入可执行状态。如果失败，G 会被放入全局队列，等待调度。</p></li></ol><p>这个过程展示了 Go 语言 runtime 中调度器如何高效地管理并调度 goroutine，最大化 CPU 资源的利用率，并避免阻塞操作影响其他 goroutine 的执行。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;&lt;code&gt;go func()&lt;/code&gt; 创建一个 goroutine 的执行流程，具体过程如下：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;p&gt;创建 goroutine ：通过 &lt;code&gt;go func()&lt;/code&gt; 来创建一个新的 goroutine。&lt;/p&gt;
&lt;/li&gt;
</summary>
      
    
    
    
    <category term="GO八股文" scheme="http://wuchuuuuu.github.io/categories/GO%E5%85%AB%E8%82%A1%E6%96%87/"/>
    
    
    <category term="goroutine" scheme="http://wuchuuuuu.github.io/tags/goroutine/"/>
    
    <category term="来执行" scheme="http://wuchuuuuu.github.io/tags/%E6%9D%A5%E6%89%A7%E8%A1%8C/"/>
    
  </entry>
  
  <entry>
    <title>goroutine什么情况下会阻塞</title>
    <link href="http://wuchuuuuu.github.io/2025/08/21/goroutine%E4%BB%80%E4%B9%88%E6%83%85%E5%86%B5%E4%B8%8B%E4%BC%9A%E9%98%BB%E5%A1%9E/"/>
    <id>http://wuchuuuuu.github.io/2025/08/21/goroutine%E4%BB%80%E4%B9%88%E6%83%85%E5%86%B5%E4%B8%8B%E4%BC%9A%E9%98%BB%E5%A1%9E/</id>
    <published>2025-08-21T12:08:41.000Z</published>
    <updated>2025-08-21T12:30:50.406Z</updated>
    
    <content type="html"><![CDATA[<p>在 Go 中，<code>goroutine</code> 会在以下几种情况下发生阻塞：</p><h3 id="1-等待-I-O-操作"><a href="#1-等待-I-O-操作" class="headerlink" title="1.  等待 I&#x2F;O 操作"></a>1.  等待 I&#x2F;O 操作</h3><p>   当一个 <code>goroutine</code> 发起 I&#x2F;O 操作（例如读取文件、网络请求、数据库查询等）时，除非是异步操作，<code>goroutine</code> 会被阻塞直到 I&#x2F;O 操作完成。例如，读取文件或网络时，<code>goroutine</code> 可能会阻塞直到读取完成。</p>   <figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 例子：网络请求导致 goroutine 阻塞</span></span><br><span class="line">response, err := http.Get(<span class="string">&quot;https://example.com&quot;</span>)</span><br><span class="line"><span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">    log.Fatal(err)</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">defer</span> response.Body.Close()</span><br></pre></td></tr></table></figure><h3 id="2-等待-Channel-的操作"><a href="#2-等待-Channel-的操作" class="headerlink" title="2.  等待 Channel 的操作"></a>2.  等待 Channel 的操作</h3><p>   <code>goroutine</code> 可能会在与 <code>channel</code> 的通信过程中被阻塞，具体表现为：</p><ul><li>发送数据到未缓冲的 channel ：发送方会阻塞直到有接收方接收数据。</li><li>从未缓冲的 channel 接收数据 ：接收方会阻塞直到有发送方发送数据。</li><li>缓冲的 channel ：如果缓冲区满，发送方会阻塞；如果缓冲区为空，接收方会阻塞。</li></ul>   <figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 例子：从 channel 接收数据阻塞</span></span><br><span class="line">ch := <span class="built_in">make</span>(<span class="keyword">chan</span> <span class="type">int</span>)</span><br><span class="line"><span class="keyword">go</span> <span class="function"><span class="keyword">func</span><span class="params">()</span></span> &#123;</span><br><span class="line">    ch &lt;- <span class="number">42</span> <span class="comment">// 阻塞，直到有接收方</span></span><br><span class="line">&#125;()</span><br><span class="line"></span><br><span class="line">data := &lt;-ch <span class="comment">// 阻塞，直到有发送方发送数据</span></span><br></pre></td></tr></table></figure><h3 id="3-锁（Mutex）竞争"><a href="#3-锁（Mutex）竞争" class="headerlink" title="3.  锁（Mutex）竞争"></a>3.  锁（Mutex）竞争</h3><p>   在多 <code>goroutine</code> 的情况下，如果一个 <code>goroutine</code> 需要访问某些共享资源，而该资源已经被其它 <code>goroutine</code> 锁定，那么当前 <code>goroutine</code> 就会阻塞，直到锁被释放。</p>   <figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">var</span> mu sync.Mutex</span><br><span class="line">mu.Lock()    <span class="comment">// 获得锁，进入临界区</span></span><br><span class="line"><span class="comment">// 如果其他 goroutine 调用 mu.Lock()，它们会阻塞直到锁被释放</span></span><br><span class="line">mu.Unlock()  <span class="comment">// 释放锁</span></span><br></pre></td></tr></table></figure><h3 id="4-等待条件变量（Cond）"><a href="#4-等待条件变量（Cond）" class="headerlink" title="4.  等待条件变量（Cond）"></a>4.  等待条件变量（Cond）</h3><p>   <code>sync.Cond</code> 是一种用于 <code>goroutine</code> 通知的机制，通常用于协调 <code>goroutine</code> 的状态变化。使用 <code>Cond.Wait()</code> 的 <code>goroutine</code> 会阻塞，直到收到 <code>Cond.Signal()</code> 或 <code>Cond.Broadcast()</code> 通知。</p>   <figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">var</span> mu sync.Mutex</span><br><span class="line">cond := sync.NewCond(&amp;mu)</span><br><span class="line"></span><br><span class="line"><span class="keyword">go</span> <span class="function"><span class="keyword">func</span><span class="params">()</span></span> &#123;</span><br><span class="line">    cond.L.Lock()</span><br><span class="line">    cond.Wait() <span class="comment">// 阻塞，直到收到通知</span></span><br><span class="line">    cond.L.Unlock()</span><br><span class="line">&#125;()</span><br><span class="line"></span><br><span class="line">cond.L.Lock()</span><br><span class="line">cond.Signal() <span class="comment">// 通知一个阻塞的 goroutine</span></span><br><span class="line">cond.L.Unlock()</span><br></pre></td></tr></table></figure><h3 id="5-select-语句中所有分支都被阻塞"><a href="#5-select-语句中所有分支都被阻塞" class="headerlink" title="5.  select 语句中所有分支都被阻塞"></a>5.  select 语句中所有分支都被阻塞</h3><p>   如果 <code>select</code> 语句中的所有 <code>channel</code> 操作都被阻塞，那么 <code>goroutine</code> 也会阻塞，直到其中一个 <code>channel</code> 能继续操作。</p>   <figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">select</span> &#123;</span><br><span class="line"><span class="keyword">case</span> msg := &lt;-ch1: <span class="comment">// 如果 ch1 没有数据，阻塞</span></span><br><span class="line">    fmt.Println(msg)</span><br><span class="line"><span class="keyword">case</span> msg := &lt;-ch2: <span class="comment">// 如果 ch2 没有数据，阻塞</span></span><br><span class="line">    fmt.Println(msg)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="6-time-Sleep-或者-time-After"><a href="#6-time-Sleep-或者-time-After" class="headerlink" title="6.  time.Sleep() 或者 time.After()"></a>6.  time.Sleep() 或者 time.After()</h3><p>   <code>goroutine</code> 会在调用 <code>time.Sleep()</code> 期间阻塞，直到指定的时间过期。</p>   <figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">time.Sleep(<span class="number">2</span> * time.Second) <span class="comment">// 阻塞 2 秒</span></span><br></pre></td></tr></table></figure><h3 id="7-等待系统调用（syscall）或外部依赖"><a href="#7-等待系统调用（syscall）或外部依赖" class="headerlink" title="7.  等待系统调用（syscall）或外部依赖"></a>7.  等待系统调用（syscall）或外部依赖</h3><p>   当 <code>goroutine</code> 发起系统调用，或者通过 <code>CGO</code> 调用外部库时，也可能会发生阻塞。系统调用或外部库的操作完成后，<code>goroutine</code> 才能恢复执行。</p><h3 id="8-主-goroutine-阻塞导致其他-goroutine-终止"><a href="#8-主-goroutine-阻塞导致其他-goroutine-终止" class="headerlink" title="8.  主 goroutine 阻塞导致其他 goroutine 终止"></a>8.  主 goroutine 阻塞导致其他 goroutine 终止</h3><p>   如果主 <code>goroutine</code>（即 <code>main</code> 函数）阻塞或退出，所有其他 <code>goroutine</code> 会被终止。所以，在 <code>main</code> 函数中，也可能需要防止主 <code>goroutine</code> 提前结束而导致阻塞。</p>   <figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">go</span> <span class="function"><span class="keyword">func</span><span class="params">()</span></span> &#123;</span><br><span class="line">    fmt.Println(<span class="string">&quot;This will not print if main exits early&quot;</span>)</span><br><span class="line">&#125;()</span><br><span class="line"></span><br><span class="line">time.Sleep(<span class="number">1</span> * time.Second) <span class="comment">// 防止 main 退出过早</span></span><br></pre></td></tr></table></figure><h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><p>在 Go 中，<code>goroutine</code> 可能会因为 I&#x2F;O 操作、channel 操作、锁竞争、条件变量等待、<code>select</code> 语句的阻塞、系统调用等原因被阻塞。因此在设计并发程序时，需要仔细考虑如何避免不必要的阻塞，以提高程序的并发性能。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;在 Go 中，&lt;code&gt;goroutine&lt;/code&gt; 会在以下几种情况下发生阻塞：&lt;/p&gt;
&lt;h3 id=&quot;1-等待-I-O-操作&quot;&gt;&lt;a href=&quot;#1-等待-I-O-操作&quot; class=&quot;headerlink&quot; title=&quot;1.  等待 I&amp;#x2F;O 操作</summary>
      
    
    
    
    <category term="GO八股文" scheme="http://wuchuuuuu.github.io/categories/GO%E5%85%AB%E8%82%A1%E6%96%87/"/>
    
    
    <category term="goroutine" scheme="http://wuchuuuuu.github.io/tags/goroutine/"/>
    
    <category term="cond" scheme="http://wuchuuuuu.github.io/tags/cond/"/>
    
  </entry>
  
  <entry>
    <title>mutex怎么使用，乐观和悲观锁的实现</title>
    <link href="http://wuchuuuuu.github.io/2025/08/21/mutex%E6%80%8E%E4%B9%88%E4%BD%BF%E7%94%A8%EF%BC%8C%E4%B9%90%E8%A7%82%E5%92%8C%E6%82%B2%E8%A7%82%E9%94%81%E7%9A%84%E5%AE%9E%E7%8E%B0/"/>
    <id>http://wuchuuuuu.github.io/2025/08/21/mutex%E6%80%8E%E4%B9%88%E4%BD%BF%E7%94%A8%EF%BC%8C%E4%B9%90%E8%A7%82%E5%92%8C%E6%82%B2%E8%A7%82%E9%94%81%E7%9A%84%E5%AE%9E%E7%8E%B0/</id>
    <published>2025-08-21T12:08:41.000Z</published>
    <updated>2025-08-21T12:30:50.405Z</updated>
    
    <content type="html"><![CDATA[<h3 id="Golang-中-sync-Mutex-的使用"><a href="#Golang-中-sync-Mutex-的使用" class="headerlink" title="Golang 中 sync.Mutex 的使用"></a>Golang 中 sync.Mutex 的使用</h3><p><code>sync.Mutex</code> 是 Go 中用来提供互斥锁（Mutual Exclusion）的结构体，它可以确保在同一时刻只有一个 Goroutine 访问共享资源。</p><h4 id="sync-Mutex-的基本用法："><a href="#sync-Mutex-的基本用法：" class="headerlink" title="sync.Mutex 的基本用法："></a>sync.Mutex 的基本用法：</h4><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> (</span><br><span class="line">    <span class="string">&quot;fmt&quot;</span></span><br><span class="line">    <span class="string">&quot;sync&quot;</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> (</span><br><span class="line">    counter <span class="type">int</span></span><br><span class="line">    mu      sync.Mutex</span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">increment</span><span class="params">(wg *sync.WaitGroup)</span></span> &#123;</span><br><span class="line">    mu.Lock()   <span class="comment">// 加锁</span></span><br><span class="line">    counter++</span><br><span class="line">    mu.Unlock() <span class="comment">// 解锁</span></span><br><span class="line">    wg.Done()</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    <span class="keyword">var</span> wg sync.WaitGroup</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> i := <span class="number">0</span>; i &lt; <span class="number">1000</span>; i++ &#123;</span><br><span class="line">        wg.Add(<span class="number">1</span>)</span><br><span class="line">        <span class="keyword">go</span> increment(&amp;wg)</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    wg.Wait()</span><br><span class="line">    fmt.Println(<span class="string">&quot;Final Counter:&quot;</span>, counter)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="使用步骤："><a href="#使用步骤：" class="headerlink" title="使用步骤："></a>使用步骤：</h3><ol><li>加锁 ：通过 <code>mu.Lock()</code> 加锁，在访问共享资源前调用，确保其他 Goroutine 无法同时访问资源。</li><li>解锁 ：通过 <code>mu.Unlock()</code> 解锁，确保在完成操作后释放锁，让其他 Goroutine 可以访问。</li></ol><h3 id="乐观锁与悲观锁"><a href="#乐观锁与悲观锁" class="headerlink" title="乐观锁与悲观锁"></a>乐观锁与悲观锁</h3><h4 id="1-悲观锁"><a href="#1-悲观锁" class="headerlink" title="1.  悲观锁"></a>1.  悲观锁</h4><p>悲观锁是一种假设冲突将频繁发生的锁定机制。在进行数据操作前，通过加锁来确保不发生冲突，其他操作必须等待锁释放后才能继续。</p><h5 id="Go-中的悲观锁实现："><a href="#Go-中的悲观锁实现：" class="headerlink" title="Go 中的悲观锁实现："></a>Go 中的悲观锁实现：</h5><p>悲观锁的实现通常直接通过 <code>sync.Mutex</code> 或 <code>sync.RWMutex</code> 来完成。它假设并发冲突会发生，因此每次访问资源时都会加锁。</p><p> 使用场景 ：</p><ul><li>数据竞争严重或并发冲突频繁的场景。</li><li>例如银行系统的资金转账、数据库写操作等，需要保证绝对一致性。</li></ul><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">mu.Lock()</span><br><span class="line"><span class="comment">// 操作临界资源</span></span><br><span class="line">mu.Unlock()</span><br></pre></td></tr></table></figure><h4 id="2-乐观锁"><a href="#2-乐观锁" class="headerlink" title="2.  乐观锁"></a>2.  乐观锁</h4><p>乐观锁则假设冲突不会经常发生，在执行操作时不会立即加锁。只有在提交或修改数据时，才会检查是否有其他 Goroutine 修改了数据。如果有冲突，就回滚操作并重试。</p><h5 id="Go-中的乐观锁实现："><a href="#Go-中的乐观锁实现：" class="headerlink" title="Go 中的乐观锁实现："></a>Go 中的乐观锁实现：</h5><p>乐观锁通常通过比较和交换（CAS, Compare And Swap）的机制来实现，例如使用 <code>sync/atomic</code> 包。</p><p> 使用场景 ：</p><ul><li>并发冲突较少或数据读取操作多，写入操作少的场景。</li><li>例如大规模的分布式系统，读多写少的情况（如缓存系统）。</li></ul><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> (</span><br><span class="line">    <span class="string">&quot;fmt&quot;</span></span><br><span class="line">    <span class="string">&quot;sync/atomic&quot;</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> counter <span class="type">int64</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">increment</span><span class="params">()</span></span> &#123;</span><br><span class="line">    <span class="keyword">for</span> &#123;</span><br><span class="line">        old := atomic.LoadInt64(&amp;counter)</span><br><span class="line">        <span class="built_in">new</span> := old + <span class="number">1</span></span><br><span class="line">        <span class="keyword">if</span> atomic.CompareAndSwapInt64(&amp;counter, old, <span class="built_in">new</span>) &#123;</span><br><span class="line">            <span class="keyword">break</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    <span class="keyword">for</span> i := <span class="number">0</span>; i &lt; <span class="number">1000</span>; i++ &#123;</span><br><span class="line">        <span class="keyword">go</span> increment()</span><br><span class="line">    &#125;</span><br><span class="line">    fmt.Println(<span class="string">&quot;Final Counter:&quot;</span>, counter)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="乐观锁与悲观锁的使用场景对比："><a href="#乐观锁与悲观锁的使用场景对比：" class="headerlink" title="乐观锁与悲观锁的使用场景对比："></a>乐观锁与悲观锁的使用场景对比：</h3><ul><li>悲观锁 ：适用于写多、并发冲突严重的场景。常用于数据库系统中，对写操作的加锁控制。</li><li>乐观锁 ：适用于读多写少、并发冲突较少的场景。分布式系统、缓存系统等场景经常用乐观锁。</li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;h3 id=&quot;Golang-中-sync-Mutex-的使用&quot;&gt;&lt;a href=&quot;#Golang-中-sync-Mutex-的使用&quot; class=&quot;headerlink&quot; title=&quot;Golang 中 sync.Mutex 的使用&quot;&gt;&lt;/a&gt;Golang 中 sync.Mut</summary>
      
    
    
    
    <category term="GO八股文" scheme="http://wuchuuuuu.github.io/categories/GO%E5%85%AB%E8%82%A1%E6%96%87/"/>
    
    
    <category term="sync" scheme="http://wuchuuuuu.github.io/tags/sync/"/>
    
    <category term="counter" scheme="http://wuchuuuuu.github.io/tags/counter/"/>
    
  </entry>
  
  <entry>
    <title>sync.Pool</title>
    <link href="http://wuchuuuuu.github.io/2025/08/21/sync.Pool/"/>
    <id>http://wuchuuuuu.github.io/2025/08/21/sync.Pool/</id>
    <published>2025-08-21T12:08:41.000Z</published>
    <updated>2025-08-21T12:30:50.401Z</updated>
    
    <content type="html"><![CDATA[<p><code>sync.Pool</code> 是 Go 标准库中的一种用于临时对象缓存的并发安全的对象池。它的主要目的是减少内存分配和垃圾回收的开销，通过重用对象来提高性能。以下是 <code>sync.Pool</code> 的详细介绍：</p><h3 id="1-基本概念"><a href="#1-基本概念" class="headerlink" title="1.  基本概念"></a>1.  基本概念</h3><ul><li><code>sync.Pool</code> 是一个可以存储任意类型对象的池，用于临时存储和复用这些对象。</li><li>典型的使用场景是需要频繁分配和释放短生命周期的对象，例如在高频率请求处理中，每次请求都需要分配一些临时的结构体或切片。</li></ul><h3 id="2-工作机制"><a href="#2-工作机制" class="headerlink" title="2.  工作机制"></a>2.  工作机制</h3><ul><li>Get ：从对象池中获取一个对象。如果池中有可用对象，则直接返回；如果没有可用对象，则调用 <code>New</code> 函数（如果提供了）创建一个新对象并返回。如果 <code>New</code> 函数未提供且池中无可用对象，则返回 <code>nil</code>。</li><li>Put ：将对象放回对象池中，以便将来复用。放入池中的对象会在后续的 <code>Get</code> 操作中被复用。</li></ul><h3 id="3-主要特点"><a href="#3-主要特点" class="headerlink" title="3.  主要特点"></a>3.  主要特点</h3><ul><li>并发安全 ：<code>sync.Pool</code> 可以在多个 Goroutine 间安全地共享和复用对象，而不需要额外的锁机制。</li><li>自动清理 ：<code>sync.Pool</code> 中的对象不是永久存储的，对象池会随着垃圾回收器的运行而被清空。因此，<code>sync.Pool</code> 主要用于缓存短期对象，而非长生命周期的对象。</li><li>全局和局部存储 ：每个 <code>P</code>（Processor） 都维护一个本地的 <code>sync.Pool</code>，<code>Get</code> 操作优先从本地的池中获取对象，这减少了锁争用的可能性。只有在本地池为空时，才会访问全局池。</li></ul><h3 id="4-使用示例"><a href="#4-使用示例" class="headerlink" title="4.  使用示例"></a>4.  使用示例</h3><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> (</span><br><span class="line">    <span class="string">&quot;fmt&quot;</span></span><br><span class="line">    <span class="string">&quot;sync&quot;</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    <span class="keyword">var</span> pool = sync.Pool&#123;</span><br><span class="line">        New: <span class="function"><span class="keyword">func</span><span class="params">()</span></span> <span class="keyword">interface</span>&#123;&#125; &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="string">&quot;default&quot;</span></span><br><span class="line">        &#125;,</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    pool.Put(<span class="string">&quot;Hello&quot;</span>)</span><br><span class="line">    pool.Put(<span class="string">&quot;World&quot;</span>)</span><br><span class="line"></span><br><span class="line">    fmt.Println(pool.Get()) <span class="comment">// 输出：Hello</span></span><br><span class="line">    fmt.Println(pool.Get()) <span class="comment">// 输出：World</span></span><br><span class="line">    fmt.Println(pool.Get()) <span class="comment">// 输出：default，因为池中已经没有对象，返回New创建的对象</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="5-使用建议"><a href="#5-使用建议" class="headerlink" title="5.  使用建议"></a>5.  使用建议</h3><ul><li>适用场景 ：<code>sync.Pool</code> 适用于那些创建和销毁成本高但生命周期短的对象。</li><li>性能提升 ：在需要频繁创建小对象的场景下，使用 <code>sync.Pool</code> 可以显著减少垃圾回收的负担，提升性能。</li><li>避免滥用 ：因为 <code>sync.Pool</code> 的对象在 GC 时会被清理，所以不适用于需要长期保存的对象。如果对象创建和销毁成本很低，使用 <code>sync.Pool</code> 可能并不能带来明显的性能提升。</li></ul><p><code>sync.Pool</code> 是一个非常有用的工具，尤其是在高并发场景下。了解如何正确使用它可以帮助你在开发 Go 应用程序时实现更高的性能和更低的内存占用。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;&lt;code&gt;sync.Pool&lt;/code&gt; 是 Go 标准库中的一种用于临时对象缓存的并发安全的对象池。它的主要目的是减少内存分配和垃圾回收的开销，通过重用对象来提高性能。以下是 &lt;code&gt;sync.Pool&lt;/code&gt; 的详细介绍：&lt;/p&gt;
&lt;h3 id=&quot;1-基本</summary>
      
    
    
    
    <category term="GO八股文" scheme="http://wuchuuuuu.github.io/categories/GO%E5%85%AB%E8%82%A1%E6%96%87/"/>
    
    
    <category term="sync" scheme="http://wuchuuuuu.github.io/tags/sync/"/>
    
    <category term="pool" scheme="http://wuchuuuuu.github.io/tags/pool/"/>
    
  </entry>
  
  <entry>
    <title>slice实践以及底层实现</title>
    <link href="http://wuchuuuuu.github.io/2025/08/21/slice%E5%AE%9E%E8%B7%B5%E4%BB%A5%E5%8F%8A%E5%BA%95%E5%B1%82%E5%AE%9E%E7%8E%B0/"/>
    <id>http://wuchuuuuu.github.io/2025/08/21/slice%E5%AE%9E%E8%B7%B5%E4%BB%A5%E5%8F%8A%E5%BA%95%E5%B1%82%E5%AE%9E%E7%8E%B0/</id>
    <published>2025-08-21T12:08:41.000Z</published>
    <updated>2025-08-21T12:30:50.404Z</updated>
    
    <content type="html"><![CDATA[<h3 id="Slice-的实践与底层实现"><a href="#Slice-的实践与底层实现" class="headerlink" title="Slice 的实践与底层实现"></a>Slice 的实践与底层实现</h3><p>在 Go 语言中，<code>slice</code> 是一种非常常用的数据结构，它是对数组的一个抽象和封装。<code>slice</code> 提供了灵活的动态数组操作，底层通过数组实现。理解 <code>slice</code> 的实践和底层实现对编写高效的 Go 程序至关重要。</p><h3 id="1-Slice-的基本使用"><a href="#1-Slice-的基本使用" class="headerlink" title="1. Slice 的基本使用"></a>1. Slice 的基本使用</h3><p><code>slice</code> 是对数组的一个动态窗口，具有长度和容量两个属性：</p><ul><li>长度（len） ：当前 <code>slice</code> 包含的元素数量。</li><li>容量（cap） ：从 <code>slice</code> 的起始位置到底层数组末尾的元素数量。</li></ul><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> <span class="string">&quot;fmt&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    <span class="comment">// 创建一个数组</span></span><br><span class="line">    arr := [<span class="number">5</span>]<span class="type">int</span>&#123;<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>&#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 基于数组创建一个 slice</span></span><br><span class="line">    slice := arr[<span class="number">1</span>:<span class="number">4</span>] <span class="comment">// 包含元素 &#123;2, 3, 4&#125;</span></span><br><span class="line"></span><br><span class="line">    fmt.Println(<span class="string">&quot;Slice:&quot;</span>, slice)</span><br><span class="line">    fmt.Println(<span class="string">&quot;Length:&quot;</span>, <span class="built_in">len</span>(slice))</span><br><span class="line">    fmt.Println(<span class="string">&quot;Capacity:&quot;</span>, <span class="built_in">cap</span>(slice))</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="2-Slice-的底层实现"><a href="#2-Slice-的底层实现" class="headerlink" title="2. Slice 的底层实现"></a>2. Slice 的底层实现</h3><p>在 Go 语言中，<code>slice</code> 是一个结构体，包含三个字段：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">type</span> slice <span class="keyword">struct</span> &#123;</span><br><span class="line">    ptr *array  <span class="comment">// 指向底层数组的指针</span></span><br><span class="line">    <span class="built_in">len</span> <span class="type">int</span>     <span class="comment">// 当前 slice 的长度</span></span><br><span class="line">    <span class="built_in">cap</span> <span class="type">int</span>     <span class="comment">// 当前 slice 的容量</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ul><li><code>ptr</code> ：指向底层数组的指针。</li><li><code>len</code> ：表示 <code>slice</code> 的长度。</li><li><code>cap</code> ：表示 <code>slice</code> 的容量。</li></ul><h3 id="3-底层数组与切片的关系"><a href="#3-底层数组与切片的关系" class="headerlink" title="3. 底层数组与切片的关系"></a>3. 底层数组与切片的关系</h3><p><code>slice</code> 是基于数组的一个抽象。切片的容量（<code>cap</code>）取决于从切片的起始位置到底层数组末尾的元素数量。切片操作不会复制底层数组的数据，而是通过指针引用。</p><ul><li>共享底层数组 ：多个切片可以共享同一个底层数组，因此对一个切片的修改可能影响其他切片。</li></ul><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    arr := [<span class="number">5</span>]<span class="type">int</span>&#123;<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>&#125;</span><br><span class="line">    slice1 := arr[<span class="number">1</span>:<span class="number">4</span>]</span><br><span class="line">    slice2 := arr[<span class="number">2</span>:<span class="number">5</span>]</span><br><span class="line"></span><br><span class="line">    slice1[<span class="number">1</span>] = <span class="number">99</span> <span class="comment">// 修改 slice1 也会影响 slice2</span></span><br><span class="line"></span><br><span class="line">    fmt.Println(<span class="string">&quot;Slice1:&quot;</span>, slice1) <span class="comment">// [2, 99, 4]</span></span><br><span class="line">    fmt.Println(<span class="string">&quot;Slice2:&quot;</span>, slice2) <span class="comment">// [99, 4, 5]</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="4-Slice-的扩容机制"><a href="#4-Slice-的扩容机制" class="headerlink" title="4. Slice 的扩容机制"></a>4. Slice 的扩容机制</h3><p>当向 <code>slice</code> 中添加元素（使用 <code>append</code> 函数）时，如果超出了 <code>slice</code> 的容量，Go 运行时会自动创建一个新的底层数组，并将旧数组的元素复制到新数组中。</p><ul><li>扩容规则 ：当容量不足时，新的容量通常会按倍数增长（一般为2倍）。不过，扩容策略可能会因为不同的长度和容量而有所不同。</li></ul><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    slice := <span class="built_in">make</span>([]<span class="type">int</span>, <span class="number">0</span>, <span class="number">3</span>) <span class="comment">// 创建一个容量为 3 的 slice</span></span><br><span class="line">    slice = <span class="built_in">append</span>(slice, <span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>)</span><br><span class="line">    fmt.Println(slice, <span class="built_in">len</span>(slice), <span class="built_in">cap</span>(slice)) <span class="comment">// [1 2 3] 3 3</span></span><br><span class="line"></span><br><span class="line">    slice = <span class="built_in">append</span>(slice, <span class="number">4</span>) <span class="comment">// 超过容量，触发扩容</span></span><br><span class="line">    fmt.Println(slice, <span class="built_in">len</span>(slice), <span class="built_in">cap</span>(slice)) <span class="comment">// [1 2 3 4] 4 6</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="5-Slice-的实践技巧"><a href="#5-Slice-的实践技巧" class="headerlink" title="5. Slice 的实践技巧"></a>5. Slice 的实践技巧</h3><h4 id="1-使用-make-函数创建切片"><a href="#1-使用-make-函数创建切片" class="headerlink" title="1. 使用 make 函数创建切片"></a>1. 使用 make 函数创建切片</h4><p><code>make</code> 函数可以创建指定长度和容量的 <code>slice</code>，适用于需要明确控制容量的场景。</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">slice := <span class="built_in">make</span>([]<span class="type">int</span>, <span class="number">5</span>, <span class="number">10</span>) <span class="comment">// 长度为 5，容量为 10 的 slice</span></span><br></pre></td></tr></table></figure><h4 id="2-切片的截取与重新切片"><a href="#2-切片的截取与重新切片" class="headerlink" title="2. 切片的截取与重新切片"></a>2. 切片的截取与重新切片</h4><p><code>slice</code> 可以被重新切片（reslice），这是一个灵活且高效的操作。</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">slice := []<span class="type">int</span>&#123;<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>&#125;</span><br><span class="line">newSlice := slice[<span class="number">1</span>:<span class="number">4</span>] <span class="comment">// [2, 3, 4]</span></span><br></pre></td></tr></table></figure><h4 id="3-使用-copy-函数进行切片复制"><a href="#3-使用-copy-函数进行切片复制" class="headerlink" title="3. 使用 copy 函数进行切片复制"></a>3. 使用 copy 函数进行切片复制</h4><p><code>copy</code> 函数可以在两个切片之间复制元素，通常用于避免共享底层数组。</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">src := []<span class="type">int</span>&#123;<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>&#125;</span><br><span class="line">dst := <span class="built_in">make</span>([]<span class="type">int</span>, <span class="built_in">len</span>(src))</span><br><span class="line"><span class="built_in">copy</span>(dst, src)</span><br></pre></td></tr></table></figure><h3 id="6-常见陷阱与注意事项"><a href="#6-常见陷阱与注意事项" class="headerlink" title="6. 常见陷阱与注意事项"></a>6. 常见陷阱与注意事项</h3><ul><li>共享底层数组 ：多个切片共享同一底层数组，修改一个切片可能会影响另一个切片。</li><li>容量不足导致的扩容 ：<code>append</code> 可能导致底层数组的重新分配，导致切片之间的关系断裂。</li><li>避免过度切片 ：切片操作本质上是数组的引用，频繁的切片操作可能导致内存浪费或不可预期的行为。</li></ul><h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><p><code>slice</code> 是 Go 语言中非常强大的数据结构，它提供了动态数组的功能，同时在性能上也有很好的表现。理解 <code>slice</code> 的底层实现有助于编写高效的 Go 代码，尤其是在处理大量数据或需要高性能的并发程序时。通过实践和深度理解，能够更好地运用 <code>slice</code> 的特性并避免常见的坑。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h3 id=&quot;Slice-的实践与底层实现&quot;&gt;&lt;a href=&quot;#Slice-的实践与底层实现&quot; class=&quot;headerlink&quot; title=&quot;Slice 的实践与底层实现&quot;&gt;&lt;/a&gt;Slice 的实践与底层实现&lt;/h3&gt;&lt;p&gt;在 Go 语言中，&lt;code&gt;slice&lt;/</summary>
      
    
    
    
    <category term="GO八股文" scheme="http://wuchuuuuu.github.io/categories/GO%E5%85%AB%E8%82%A1%E6%96%87/"/>
    
    
    <category term="go" scheme="http://wuchuuuuu.github.io/tags/go/"/>
    
    <category term="slice" scheme="http://wuchuuuuu.github.io/tags/slice/"/>
    
  </entry>
  
  <entry>
    <title>乐观锁与悲观锁与Golang</title>
    <link href="http://wuchuuuuu.github.io/2025/08/21/%E4%B9%90%E8%A7%82%E9%94%81%E4%B8%8E%E6%82%B2%E8%A7%82%E9%94%81%E4%B8%8EGolang/"/>
    <id>http://wuchuuuuu.github.io/2025/08/21/%E4%B9%90%E8%A7%82%E9%94%81%E4%B8%8E%E6%82%B2%E8%A7%82%E9%94%81%E4%B8%8EGolang/</id>
    <published>2025-08-21T12:08:41.000Z</published>
    <updated>2025-08-21T12:30:50.405Z</updated>
    
    <content type="html"><![CDATA[<p>乐观锁和悲观锁是两种在并发编程中常用的锁机制，它们的主要目的是防止多个进程或线程同时操作共享资源时发生数据竞争或不一致的情况。下面我们将讨论这两种锁的概念以及在 Golang 中的实现。</p><h3 id="1-悲观锁"><a href="#1-悲观锁" class="headerlink" title="1.  悲观锁"></a>1.  悲观锁</h3><h4 id="概念"><a href="#概念" class="headerlink" title="概念"></a>概念</h4><ul><li>悲观锁（Pessimistic Lock）  假设在操作共享资源时，会有数据竞争的情况，因此在每次操作共享资源之前都会先加锁，确保其他线程不能同时访问该资源。</li><li>在使用悲观锁时，当一个线程获取锁之后，其他所有试图访问该资源的线程都会被阻塞，直到锁被释放。</li></ul><h4 id="应用场景"><a href="#应用场景" class="headerlink" title="应用场景"></a>应用场景</h4><ul><li>高冲突环境 ：悲观锁适用于并发冲突较高的场景，比如在数据库操作中，频繁的读写可能导致数据竞争。</li></ul><h4 id="在-Golang-中的实现"><a href="#在-Golang-中的实现" class="headerlink" title="在 Golang 中的实现"></a>在 Golang 中的实现</h4><ul><li><p>在 Golang 中，悲观锁通常通过 <code>sync.Mutex</code> 和 <code>sync.RWMutex</code> 实现。</p><p> <code>sync.Mutex</code> 示例 ：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">var</span> mu sync.Mutex</span><br><span class="line"><span class="keyword">var</span> count <span class="type">int</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">increment</span><span class="params">()</span></span> &#123;</span><br><span class="line">    mu.Lock()</span><br><span class="line">    <span class="keyword">defer</span> mu.Unlock()</span><br><span class="line">    count++</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在上面的例子中，<code>mu.Lock()</code> 会阻止其他 Goroutine 访问 <code>count</code> 变量，直到 <code>mu.Unlock()</code> 被调用为止。</p><p> <code>sync.RWMutex</code> 示例 ：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">var</span> rwMu sync.RWMutex</span><br><span class="line"><span class="keyword">var</span> data <span class="type">int</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">readData</span><span class="params">()</span></span> <span class="type">int</span> &#123;</span><br><span class="line">    rwMu.RLock()</span><br><span class="line">    <span class="keyword">defer</span> rwMu.RUnlock()</span><br><span class="line">    <span class="keyword">return</span> data</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">writeData</span><span class="params">(newData <span class="type">int</span>)</span></span> &#123;</span><br><span class="line">    rwMu.Lock()</span><br><span class="line">    <span class="keyword">defer</span> rwMu.Unlock()</span><br><span class="line">    data = newData</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在上面的例子中，<code>RWMutex</code> 允许多个 Goroutine 同时读操作（使用 <code>RLock()</code>），但写操作（使用 <code>Lock()</code>）会阻塞其他读和写操作。</p></li></ul><h3 id="2-乐观锁"><a href="#2-乐观锁" class="headerlink" title="2.  乐观锁"></a>2.  乐观锁</h3><h4 id="概念-1"><a href="#概念-1" class="headerlink" title="概念"></a>概念</h4><ul><li>乐观锁（Optimistic Lock）  假设在操作共享资源时，数据竞争是非常少见的，因此在操作资源时不立即加锁，而是在提交操作前检查资源是否发生了变化。如果资源没有变化，则提交操作；如果资源已经被修改，则操作失败或重试。</li><li>乐观锁通常使用版本号或时间戳来检测资源是否被修改。</li></ul><h4 id="应用场景-1"><a href="#应用场景-1" class="headerlink" title="应用场景"></a>应用场景</h4><ul><li>低冲突环境 ：乐观锁适用于并发冲突较低的场景，比如在数据库操作中，读多写少的情况下。</li></ul><h4 id="在-Golang-中的实现-1"><a href="#在-Golang-中的实现-1" class="headerlink" title="在 Golang 中的实现"></a>在 Golang 中的实现</h4><ul><li><p>在 Golang 中，乐观锁的实现通常依赖于 CAS（Compare-And-Swap）操作。Go 的标准库 <code>sync/atomic</code> 提供了一些原子操作函数，可以用来实现乐观锁。</p><p> <code>sync/atomic</code> 示例 ：</p><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> (</span><br><span class="line">    <span class="string">&quot;sync/atomic&quot;</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> counter <span class="type">int64</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">increment</span><span class="params">()</span></span> &#123;</span><br><span class="line">    <span class="keyword">for</span> &#123;</span><br><span class="line">        oldValue := atomic.LoadInt64(&amp;counter)</span><br><span class="line">        newValue := oldValue + <span class="number">1</span></span><br><span class="line">        <span class="keyword">if</span> atomic.CompareAndSwapInt64(&amp;counter, oldValue, newValue) &#123;</span><br><span class="line">            <span class="keyword">break</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在上面的例子中，<code>CompareAndSwapInt64</code> 是一个典型的乐观锁操作。如果 <code>counter</code> 的值在读取后没有变化，那么 <code>CompareAndSwapInt64</code> 会将其更新为新值，并返回 <code>true</code>，否则返回 <code>false</code> 并重试。</p></li></ul><h3 id="3-悲观锁-vs-乐观锁"><a href="#3-悲观锁-vs-乐观锁" class="headerlink" title="3.  悲观锁 vs. 乐观锁"></a>3.  悲观锁 vs. 乐观锁</h3><ul><li><p>悲观锁 ：</p></li><li><p>优势 ：能有效防止并发冲突，适用于高并发写操作的场景。</p></li><li><p>劣势 ：可能导致较高的锁争用，降低系统的并发性能。</p></li><li><p>乐观锁 ：</p></li><li><p>优势 ：在并发冲突较低时，性能更好，适用于读多写少的场景。</p></li><li><p>劣势 ：在冲突频繁时，可能导致大量重试操作，反而降低性能。</p></li></ul><h3 id="4-总结"><a href="#4-总结" class="headerlink" title="4.  总结"></a>4.  总结</h3><ul><li>悲观锁 ：通过在操作共享资源时主动加锁来避免并发冲突，适用于高冲突的环境。在 Go 中通过 <code>sync.Mutex</code> 和 <code>sync.RWMutex</code> 实现。</li><li>乐观锁 ：假设并发冲突不频繁，通过在操作完成时检查冲突来避免数据不一致，适用于低冲突的环境。在 Go 中可以通过 <code>sync/atomic</code> 提供的 CAS 操作来实现。</li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;p&gt;乐观锁和悲观锁是两种在并发编程中常用的锁机制，它们的主要目的是防止多个进程或线程同时操作共享资源时发生数据竞争或不一致的情况。下面我们将讨论这两种锁的概念以及在 Golang 中的实现。&lt;/p&gt;
&lt;h3 id=&quot;1-悲观锁&quot;&gt;&lt;a href=&quot;#1-悲观锁&quot; class=&quot;</summary>
      
    
    
    
    <category term="GO八股文" scheme="http://wuchuuuuu.github.io/categories/GO%E5%85%AB%E8%82%A1%E6%96%87/"/>
    
    
    <category term="sync" scheme="http://wuchuuuuu.github.io/tags/sync/"/>
    
    <category term="atomic" scheme="http://wuchuuuuu.github.io/tags/atomic/"/>
    
  </entry>
  
</feed>
